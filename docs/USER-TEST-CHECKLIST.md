# v0.15.8 사용자 실사용 체크리스트

v0.15.8은 실제 화면 위 코치마크 구조를 유지하면서 이전 단계 이동과 변경 비교의 강조 누락을 보완합니다. 아래 항목은 다음 대규모 사용자 검토 때 확인하면 되며, 샌드박스 자동 검증은 별도로 수행합니다.

## v0.15.8 이전 이동·비교 강조

- [ ] 설명과 `잘했어요!` 결과에서 하단 좌측 `이전`, 하단 우측 `다음`이 보이고 둘 다 기능 탭과 겹치지 않는다.
- [ ] 밝은 직접 체험 화면에서도 좌측 하단 `이전`이 보이며, 누르면 같은 조작을 다시 강요하지 않고 직전 단계의 결과 또는 설명으로 돌아간다.
- [ ] 첫 단계의 `이전`은 비활성화되어 있고 튜토리얼 밖으로 나가거나 잘못된 단계로 이동하지 않는다.
- [ ] 320px·390px 모바일에서 이전·다음, 설명 dock, 강조 target, 하단 기능 탭이 서로 가리지 않는다.
- [ ] 변경 비교에서 기준과 비교 스냅샷 선택 control이 각각 노란 ring으로 강조된다.
- [ ] `추가`, `수정`, `삭제` 단계에서 제목 일부가 아니라 해당 카드 전체가 강조되고 한 번 펼치면 `잘했어요!`로 이동한다.
- [ ] `전체 프롬프트 문자 변경 보기`도 강조가 보이며 비동기 비교 계산 뒤 ring이 사라지지 않는다.
- [ ] 화면 회전·창 크기 변경 뒤에도 현재 강조 target이 화면 안으로 다시 이동하고 이전 control과 겹치지 않는다.

## v0.15.7 직접 체험 흐름 보정

- [ ] 설명·체험·결과 어느 화면에서도 나가기 control은 우측 상단 한 곳에만 보이고 좌측 하단에는 표시되지 않는다.
- [ ] 읽기 전용 설명에서는 `다음`이 보이지만, 조작 단계로 들어가면 `직접 해보기`를 따로 누르지 않고 설명과 강조된 실제 control이 곧바로 함께 보인다.
- [ ] 조작 단계의 dock에서 기능 이름, 무엇을 하는지, 언제 쓰는지와 지금 할 일을 순서대로 읽을 수 있다.
- [ ] 강조된 실제 control을 한 번 조작하면 별도 `다음` 없이 `잘했어요!` 결과 화면으로 자동 전환한다.
- [ ] 프리셋 표시 범위가 `전체 프리셋`인 상태에서 한 번 누르면 `요청 포함만`으로 바뀌면서 즉시 완료되고, 다시 전체 프리셋으로 되돌려야 완료되는 현상이 없다.
- [ ] 튜토리얼을 닫거나 다음 단계로 이동한 직후 이전 control의 지연 이벤트가 새 단계를 완료시키지 않는다.
- [ ] 320px·390px 모바일에서 practice dock이 가로로 넘치거나 내부 스크롤을 만들지 않고 실제 target과 종료 control을 가리지 않는다.
- [ ] 강한 호스트 테마에서도 설명용 종료와 practice 종료가 동시에 두 개 보이지 않는다.

## v0.15.6 코치마크 보완

- [ ] 시작 초대가 JSON·XML을 맥락 없이 먼저 언급하지 않고, 예상과 다른 답변의 원인을 연습 화면에서 찾는다는 목표를 쉽게 설명한다.
- [ ] 320px·390px 모바일에서 암전된 실제 화면 위에 target ring, 점선 화살표, 제목과 두 개의 짧은 설명이 보이며 흰색 설명 sheet나 내부 스크롤이 없다.
- [ ] 우측 상단에는 원형 종료 버튼 하나만, 하단에는 작은 `현재 / 38`과 원형 다음 화살표만 보이고 텍스트 버튼 묶음이나 장별 진행 막대가 없다.
- [ ] target이 작은 캡처 점이어도 상태 문구를 포함한 최소 52×44px 영역이 밝게 강조되어 즉시 눈에 들어온다.
- [ ] 상호작용 단계는 별도 `직접 해보기` 화살표 없이 dim이 사라진 실제 화면, 두 번만 pulse하는 target ring과 설명·지시 dock으로 바로 시작한다.
- [ ] 캡처 실습에는 아이콘만 있는 원형 control이 아니라 `연습 캡처 시작` 이름이 보이는 버튼이 있고 실행 중 상태도 구분된다.
- [ ] 320px에서 캡처 지시와 실행 버튼이 세로로 배치되어 문구나 버튼이 찌그러지거나 가로로 넘치지 않는다.
- [ ] 실제 button·input·select·summary를 한 번 조작하면 별도 확인 동작 없이 녹색 결과 코치마크로 자동 전환한다.
- [ ] 프리셋 표시 범위가 이미 `요청 포함만`인 상태로 단계에 들어가도 껐다 다시 켤 필요 없이 현재 상태를 인식한다.
- [ ] 묶음이나 카드를 펼치면 결과 화면에서 제목 줄만이 아니라 새로 펼쳐진 본문 영역이 녹색 ring으로 강조된다.
- [ ] 상호작용 완료 화면이 이전 설명과 명확히 달라 보이며 `잘했어요!`, 방금 한 단계의 이름과 관찰 가능한 결과를 순서대로 보여 준다.
- [ ] 직접 조작할 것이 없는 설명 단계는 fake practice·`확인했어요`·중복 결과 화면 없이 다음 화살표 한 번으로 넘어간다.
- [ ] 캡처 연습 버튼을 누르면 `대기 → 감지 → 저장 중 → 저장됨`이 보이고 결과 설명으로 자동 전환한다.
- [ ] 코치마크·spotlight·성공 mark의 전환이 짧고 부드럽지만 기다림을 만들지 않으며, reduced-motion 환경에서는 움직임 없이 상태만 바뀐다.
- [ ] 밝은/어두운/강한 사용자 테마에서 target ring·화살표·본문·원형 버튼을 색상뿐 아니라 모양으로 구분할 수 있다.
- [ ] Escape와 우측 상단 원형 종료 버튼이 modal·`inert`·target 표시를 제거하고 시작 전 focus와 live view를 복구한다.
- [ ] 실습 중 실제 snapshot store·provider·Semantic Inspector·clipboard·export 호출이 발생하지 않고 연습 snapshot이 live timeline에 남지 않는다.
- [ ] 화면 읽기 도구가 단계 제목·진행 숫자·종료/다음/실행 버튼 이름을 읽으며 practice에서 실제 target의 설명을 연결한다.

## v0.15.4 기반 단계 흐름 회귀 항목

- [ ] 처음 업데이트한 환경에서 초대 dialog가 한 번 열리고, 시작 전 제품 영역만 `inert`이며 `나중에` 또는 `전체 안내 건너뛰기`로 즉시 닫을 수 있다.
- [ ] 읽기 전용 briefing에서는 화면이 충분히 어두워지고 현재 실제 target만 밝은 spotlight와 뚜렷한 경계로 구분된다.
- [ ] 읽기 전용 briefing에는 이번 단계의 목적·찾을 위치·짧은 설명만 보여서 스크롤 없이 핵심을 읽을 수 있고 긴 기능 설명이 한꺼번에 밀려오지 않는다.
- [ ] 읽기 전용 briefing 동안 제품 영역은 `inert`이고 keyboard focus가 안내 밖으로 빠지지 않으며, 화면 읽기 도구가 dialog 제목과 현재 단계 번호를 안내한다.
- [ ] 상호작용 단계는 practice로 바로 시작하고 dim·spotlight modal 없이 실제 제품 화면이 정상 밝기와 대비로 보인다.
- [ ] practice에는 compact dock으로 기능 의미·사용 맥락·현재 할 일과 완료 기준을 보여 주며 읽기 영역이 제품 화면 대부분을 가리지 않는다.
- [ ] practice에서 실제 button·input·select 또는 disclosure `summary`를 mouse·touch·keyboard로 직접 조작할 수 있다.
- [ ] 현재 단계와 관계없는 control도 click/focus가 차단되지 않고 잘못 누른 control 때문에 단계가 임의 완료되거나 실제 데이터가 변경되지 않는다.
- [ ] 정확한 click/change/input/toggle을 수행하면 debrief로 전환되고 화면이 다시 dim된 상태에서 방금 확인한 target·결과·의미·다음 조사 연결이 보인다.
- [ ] debrief의 추가 설명은 방금 한 행동과 결과를 구체적으로 설명하며 같은 briefing 문구를 그대로 반복하지 않는다.
- [ ] `이전`·`다음`·`이 단계 건너뛰기`·`전체 안내 건너뛰기`·`완료`가 phase를 건너 잘못 남기지 않고, 전환 뒤 focus가 새 surface 또는 실제 target으로 이동한다.
- [ ] 읽기 전용 단계는 briefing 한 번, 상호작용 단계는 practice → debrief 순서를 사용하며 이전 단계의 dim·spotlight·task dock이 다음 단계에 겹쳐 남지 않는다.
- [ ] 캡처 → 전송 프롬프트 → 규칙 검사 → 기록 → 변경 비교 → 검색의 6개 장과 전체/장별 진행률을 현재 안내에서 확인할 수 있다.
- [ ] 캡처 3·전송 프롬프트 10·규칙 검사 7·기록 6·변경 비교 7·검색 5의 총 38단계가 빠지거나 중복되지 않는다.
- [ ] 모든 장이 `답변 형식이 JSON에서 XML로 바뀐 이유 찾기` 한 사건을 이어 가며, 각 장의 질문과 현재 단계의 할 일이 다음 조사 행동을 구체적으로 설명한다.
- [ ] 320px 및 390px 너비에서 안내 sheet가 190~260px 높이에 잘리지 않고 내용에 맞춰 커지며 제목·설명·task·결과·버튼을 문장 단위로 읽을 수 있다.
- [ ] 작은 화면에서 sheet 내용이 viewport보다 길 때만 본문이 내부 스크롤되고 하단 제품 내비게이션·주요 버튼·safe area를 가리거나 가로 overflow를 만들지 않는다.
- [ ] 밝은·어두운·SillyTavern 강한 사용자 테마에서 dim 아래 제품과 spotlight target, 안내 sheet 본문·버튼, practice task dock을 서로 구분할 수 있다.
- [ ] 캡처 연습 전 헤더 상태에 `대기` copy와 pill이 보이고 실행하면 `감지 → 저장 중 → 저장됨`이 순서대로 색상 점·copy·pill에 함께 표시된다.
- [ ] 캡처 상태는 색을 보지 않아도 네 상태를 구분할 수 있고 화면 읽기 도구가 각 전환을 중복 없이 안내한다.
- [ ] 시작 직후에는 920·1,080 토큰 스냅샷 2개만 보이고 캡처 연습 뒤 session 안에만 1,248 토큰 세 번째 스냅샷이 추가된다.
- [ ] 모든 장에 `연습 데이터 · 저장·전송 안 함` 상태가 보이고 실제 snapshot store 추가·삭제·보관, provider·Semantic Inspector, 클립보드, export 호출이 발생하지 않는다.
- [ ] 실습 전후의 live 탭·선택된 스냅샷·타임라인·캡처 상태가 같고 실습 중 도착한 실제 캡처는 종료 뒤 보이지만 연습 스냅샷은 남지 않는다.
- [ ] 종료하거나 전체 안내를 건너뛰면 modal·`inert`·spotlight·task dock이 모두 제거되고 시작 전 live view와 keyboard focus가 복구된다.
- [ ] 저장된 최소 온보딩 상태에는 v4와 건너뛰기 또는 완료만 남고 현재 단계·phase·시각·연습 원문은 남지 않는다.

## v0.15.1 hands-on 온보딩 v2

- [ ] 처음 업데이트한 환경에서 패널을 열면 `8~12분 동안 직접 실습해 볼까요?`가 한 번 제안되고 `전체 안내 건너뛰기`로 즉시 닫을 수 있다.
- [ ] 헤더의 나침반 아이콘 `사용 안내 둘러보기`로 건너뛴 뒤에도 다시 시작할 수 있다.
- [ ] 캡처 3단계 → 전송 프롬프트 10단계 → 규칙 검사 7단계 → 기록 6단계 → 변경 비교 7단계 → 검색 5단계의 6개 주제·38단계가 진행된다.
- [ ] 각 단계에 `무엇인가요?`·`언제 쓰나요?`·직접 수행할 `할 일`이 구분되어 보인다.
- [ ] click/change/input/toggle이 필요한 단계마다 `이 단계 건너뛰기`가 보이고, 건너뛰면 다음 단계로 안전하게 이동한다.
- [ ] 시작 직후 연습 타임라인에는 920·1,080 토큰 스냅샷 2개만 보이고 캡처 연습 버튼을 누르면 `capturing → processing → saved` 뒤 1,248 토큰의 세 번째 스냅샷이 추가된다.
- [ ] 전송 프롬프트에서 요청 포함 필터·소스/provenance·최종 프롬프트·payload를 실제 control로 열고 선택할 수 있다.
- [ ] 규칙 검사에서 JSON/XML 형식 충돌과 관련 소스를 확인하고, 기록에서 성장 그래프 점과 스냅샷 목록을 조작할 수 있다.
- [ ] 변경 비교에서 추가·수정·삭제가 각각 보이고, 검색에 `한국어`를 입력하면 Main Prompt 결과를 열 수 있다.
- [ ] 모든 화면에 `연습 데이터로 동작 중입니다. 저장하거나 AI에 전송하지 않습니다.`가 계속 표시된다.
- [ ] 실습 전후의 live 탭·선택된 스냅샷·타임라인·저장 개수가 같고 실제 snapshot store 추가/삭제, provider 요청, 클립보드 복사, 내보내기가 실행되지 않는다.
- [ ] 실습 중 실제 캡처가 도착한 경우 종료 뒤 해당 live 변경이 보이며 연습 스냅샷은 live 타임라인에 남지 않는다.
- [ ] `이전`·`다음`·`완료`·`전체 안내 건너뛰기`와 Escape가 작동하고 닫은 뒤 원래 live view와 focus가 복구된다.
- [ ] 모바일에서 안내 패널이 하단 기능 내비게이션을 가리지 않고 가로로 넘치지 않으며, 데스크톱에서는 강조 대상 근처에 표시된다.
- [ ] 밝은·어두운·강한 사용자 테마에서 설명·제품 화면·강조 테두리를 읽을 수 있다.
- [ ] AI 검사나 공식 Provider 평가가 진행 중이면 hands-on 실습이 겹쳐 열리지 않는다.

## v0.14.1 최종 검토 예약

- [ ] ST DevTools 제목에 `v0.15.8`이 표시된다.
- [ ] 규칙 검사 AI 연결 설정 안의 `고급: 공식 합성 Provider 평가`는 기본 접힘이며 일반 검사 흐름을 가리지 않는다.
- [ ] `1회`를 선택하면 16건, `3회`를 선택하면 48건과 최대 응답 상한 합계가 호출 전에 표시된다.
- [ ] corpus v2·16건·SHA-256 digest와 실제 relation target 2건, atom bridge 1건, source bridge 13건이 표시된다.
- [ ] 한 번 버튼을 누를 때 한 사례의 미리보기만 열리고 case ID·반복·순서·provider/model·digest·합성 원문이 보인다.
- [ ] 각 미리보기의 동의 체크는 항상 빈 상태이며 취소하면 provider 호출과 자동 다음 실행 없이 세션이 종료된다.
- [ ] 선택한 Connection Manager 프로필이 미리보기 뒤 사라져도 current 연결을 호출하지 않고 identity 변경 오류로 중단한다.
- [ ] 빠르게 실행 버튼을 두 번 눌러도 미리보기·동의·provider 전송은 한 번만 시작된다.
- [ ] 실패 화면에서도 동의 수·전송 시도 수·평가 완료 수가 구분되며 이미 시도된 요청이 `0건`으로 숨겨지지 않는다.
- [ ] 취소·timeout 뒤 underlying 호출이 끝나는 동안 새 세션 버튼이 잠기고 중복 비용 가능성이 안내된다.
- [ ] 평가 중 일반 AI 의미 검사를 동시에 시작할 수 없고, 평가 패널을 닫아도 자동으로 다음 사례가 시작되지 않는다.
- [ ] 완료 화면에는 집계율과 안정된 오류만 보이며 raw response·evidence quote·opaque profile ID를 복사하거나 내보내는 기능이 없다.
- [ ] 1회 기준을 만족해도 `연결 점검 통과`로만 표시되고 공식 품질 통과는 같은 provider·model·route의 3회 전체 통과에서만 표시된다.
- [ ] 프롬프트 카드를 펼쳐 `원문 복사`를 누르면 화면에 보이는 공백·줄바꿈·`{{...}}` 매크로까지 그대로 클립보드에 들어가고 성공 알림이 표시된다.
- [ ] `생성 설정`과 `프롬프트 payload`의 `내용 복사`가 화면과 같은 JSON 또는 텍스트를 복사한다.
- [ ] AI 결과의 `제안 내용 복사`는 제목·요약·판단 이유를 복사하며 완성된 수정 프롬프트로 오인되는 이름을 사용하지 않는다.
- [ ] redacted·metadata 스냅샷에서는 원문이 있는 것처럼 복사 버튼이 나타나지 않는다.
- [ ] 캐릭터 설명·성격과 사용자 페르소나에 `이름, 나이, 외모, 성격, 취향`처럼 같은 프로필 항목과 안내 문장을 사용해도 그 구조만을 근거로 중복·충돌 경고가 생기지 않는다.
- [ ] 같은 긴 지시문을 캐릭터 시나리오와 시스템 또는 사용자 지정 프롬프트에 실제로 반복하면 중복 경고가 계속 표시된다.
- [ ] 밝음·어두움 모드와 모바일 폭에서 복사 버튼이 카드 밖으로 넘치거나 호스트 테마 때문에 가로 전체 버튼으로 늘어나지 않는다.

## v0.13.0 AI 회귀 검토

- [ ] 사용하는 실제 Connection Manager 프로필 또는 현재 연결에서 AI 검사가 완료되고, OpenAI 계열·Anthropic·Google 계열 중 해당 provider의 정상 결과가 `AI 제안`으로 표시된다.
- [ ] AI 제안의 근거 위치에는 `source:...` 같은 내부 ID가 아니라 사용자가 보는 프롬프트 이름이 표시된다.
- [ ] 인증 오류·요청 한도·네트워크 단절·timeout·provider 일시 장애 중 재현 가능한 실패에서 서로 다른 한국어 설명과 `SEMANTIC_*` 진단 코드가 표시된다.
- [ ] provider가 content filter 또는 safety 거부를 반환하면 `잘못된 JSON`으로만 설명하지 않고 provider 거부로 안내하며, provider의 원문 오류·response body는 패널에 노출되지 않는다.
- [ ] 화면 읽기 도구를 사용하면 AI 검사 준비·동의 대기·실행·완료 또는 오류 상태가 status로 안내되고 실행 중 영역이 busy로 전달된다.
- [ ] 샌드박스의 `AI 요청 한도 오류 fixture`에서 `SEMANTIC_RATE_LIMITED · provider-rate-limited` 진단을 확인할 수 있다.

합성 평가 corpus v2 16건과 유용성·오탐률·근거 범위 pair 적중률, 각 의미 축의 양성 issue·근거 pair 완전 적중·음성 무제안 gate는 자동 테스트 대상입니다. 특정 실제 모델의 품질을 통과로 보증하는 항목이 아니므로 사용자가 지금 API 비용을 들여 corpus 전체를 반복 실행할 필요는 없습니다. 나중에 실제 provider를 평가할 때는 [`SEMANTIC-PROVIDER-MANUAL-EVALUATION.md`](SEMANTIC-PROVIDER-MANUAL-EVALUATION.md)의 개인정보·동의·비용·중단 기준을 사용합니다.

## v0.12.4 회귀 검토

- [ ] 캐릭터 그리팅이 요청에 포함되지 않은 스냅샷에서 `연결 위치를 확인하지 못한 활성 소스` 근거에 캐릭터 첫 메시지가 나타나지 않는다.
- [ ] 모바일 설정에서 섹션·필드 사이가 이전보다 촘촘하며, 고급 설정 제목의 터치 영역은 여전히 충분하다.

## v0.12.3 회귀 검토

- [ ] 툴팁 `?` 아이콘이 제목 중앙선에 맞고 위로 쏠리지 않는다.
- [ ] 일반 소스의 구조 위치에는 작은 조준 아이콘만 보이며 최종 프롬프트 자체의 구조 위치에는 이 아이콘이 없다.
- [ ] 규칙 검사에서 `최종 프롬프트 근거 보기`를 누르면 프롬프트 그룹과 최종 프롬프트 카드가 접혀 있어도 펼쳐지고 실제 근거가 중앙으로 이동한다.
- [ ] AI 검사 실행 중 다른 기능으로 이동했다 돌아와도 실행 상태가 유지되고 결과가 이어서 표시된다.
- [ ] AI 연결 설정에서 추가 검사 프롬프트와 응답 프리필을 저장한 뒤 실행 동의 화면에서 고정 지시·추가 지시·프리필을 각각 확인할 수 있다.
- [ ] AI 응답이 폐기되면 `형식 오류` 한 줄만 보이는 대신 인용·스키마 등 bounded 세부 사유가 표시된다.
- [ ] 일반 설정의 사용자 가격표가 사라지고 예전 로컬 가격표가 비용 계산에 사용되지 않는다.
- [ ] 새 Chat Completion 요청은 연결 설정의 최대 컨텍스트를 읽어 사용률과 남은 컨텍스트를 계산한다. 기존 스냅샷은 값이 없으면 계속 미확인일 수 있다.
- [ ] 상단 새로고침 버튼을 누르면 아이콘이 회전하고 완료 또는 실패 알림이 표시된다.
- [ ] 전송 프롬프트 카드의 제공자와 모델이 서로 다른 줄로 표시된다.
- [ ] 최근 토큰 값이 비슷하면 그래프에 `변화 확대`가 표시되어 작은 차이를 구분할 수 있고, 큰 격차가 포함되면 0 기준 눈금으로 자동 복귀한다.
- [ ] 강한 SillyTavern 테마에서도 체크박스 안에 두 번째 체크박스나 의사 요소가 겹치지 않는다.

## 시작 전

- [ ] ST DevTools 제목에 `v0.15.8`이 표시된다.
- [ ] 중요한 스냅샷은 먼저 `전체 원문 백업`으로 내려받아 공유되지 않는 위치에 보관한다.
- [ ] AI·lifecycle·복원 검사는 실제 보관분과 분리한 테스트 채팅 또는 테스트 브라우저 프로필에서 한다.
- [ ] SillyTavern 버전, 브라우저, Chat/Text Completion, 선택 source와 model을 기록한다.
- [ ] 실제 API key·개인정보·비공개 캐릭터 설정이 없는 짧은 테스트 프롬프트와 감당 가능한 응답 토큰 상한을 준비한다.

## v0.12.2 다섯 기능 내비게이션·프롬프트 흐름

- [ ] 하단에 `프롬프트`·`기록`·`비교`·`검사`·`검색` 다섯 아이콘과 짧은 라벨이 한꺼번에 보이고 현재 기능 하나만 선택 상태로 구분된다.
- [ ] 각 항목을 한 번 누르면 각각 `전송 프롬프트`·`기록`·`변경 비교`·`규칙 검사`·`프롬프트 검색`으로 바로 이동하며 `요청 상세`나 중간 하위 탭이 나타나지 않는다.
- [ ] `전송 프롬프트` 첫 화면에서 이번 요청의 총 토큰·컨텍스트 사용률·남은 컨텍스트·provider/model과 현재 요청 선택기를 한 장에서 확인할 수 있다.
- [ ] `프롬프트`와 `기록`, `비교`와 `검사`, `검색`을 번갈아 열어도 다른 기능의 마지막 선택을 기억해 엉뚱한 화면을 열지 않는다.
- [ ] 프리셋 프롬프트 그룹은 처음에 접혀 있고 직접 펼치면 Prompt Manager 순서의 전체 프리셋을 확인할 수 있다.
- [ ] `요청 포함만`을 켜면 `요청 포함` 상태인 프리셋만 남고, 끄면 비활성·이번 요청 미포함 항목을 포함한 전체 프리셋이 같은 순서로 복원된다.
- [ ] 접힌 최종 프롬프트 상태에서 소스의 `최종 위치 보기`를 누르면 최종 프롬프트가 열리고 정확한 범위가 화면 중앙으로 이동한다.
- [ ] `최종 위치 보기`는 연결 방식 줄 오른쪽의 실제 버튼으로 구분되고, 소스 카드와 최종 프롬프트의 대응 범위가 같은 식별 색으로 연결된다.
- [ ] 구조 위치 도움말은 카드나 패널 밖으로 잘리거나 내부 가로 스크롤을 만들지 않고 화면 안에서 문장 단위로 줄바꿈된다.
- [ ] 전송 프롬프트 아래에서 접힌 `생성 설정`과 `프롬프트 payload`를 열 수 있으며 독립 `요청 상세` 탭은 존재하지 않는다.
- [ ] 각 화면의 실제 기능 이름이 콘텐츠 위 제목으로 보이고 제목 옆 도움말에서 PC hover·키보드 focus·모바일 click으로 짧은 설명을 확인할 수 있다.
- [ ] 콘텐츠를 길게 스크롤해도 하단 기능 내비게이션은 움직이거나 가려지지 않고, 기능을 바꾸면 새 콘텐츠의 처음으로 이동한다.
- [ ] 각 기능에 처음 들어갔을 때 가장 중요한 다음 행동이나 결과가 먼저 보이며 고급 설정·상세 데이터가 초기 화면을 밀어내지 않는다.
- [ ] 헤더의 `ST DevTools` 이름 옆 상태 점과 짧은 문구가 대기·처리·저장·실패를 함께 표현해 색만으로 상태를 구분하지 않아도 된다.
- [ ] 카드와 disclosure의 요약 제목이 좌측 정렬되고, 도움말 아이콘이 제목보다 두드러지지 않으면서 PC hover·키보드 focus·모바일 click으로 열 수 있다.
- [ ] 첫 스냅샷이 없는 화면에는 빠른 시작이 나타나지만 이미 스냅샷이 있는 일반 화면에는 긴 설명이 반복되지 않는다.
- [ ] SillyTavern 밝은/어두운 테마와 ST DevTools 자동·밝게·어둡게 모드 조합에서 본문·상태색·버튼·표의 대비가 유지된다.
- [ ] 버튼 전체 폭, 세로 writing mode, table 글자색처럼 강한 사용자·호스트 테마 규칙을 적용해도 패널 안 탭·버튼·select·상태색·비교 표가 무너지지 않는다.
- [ ] 어두운 패널에서 호스트 테마가 흰 select·input 배경을 `!important`로 강제해도 입력 배경과 글자가 같은 색으로 겹치지 않는다.
- [ ] 설정을 열 때 첫 입력칸에 커서나 모바일 키보드가 자동으로 뜨지 않고, 상단 닫기 버튼으로 바로 나갈 수 있다.
- [ ] 보관 수·불러올 수·개인정보·테마처럼 자주 쓰는 설정은 바로 보이고, 드물게 쓰는 고급 설정만 사용자가 펼치며 고급 설정의 좌우 여백이 카드 경계에 붙지 않는다.
- [ ] 설정에서 테마를 바꾸면 적용 버튼까지 스크롤하지 않아도 즉시 저장되고 패널을 닫았다 다시 열어도 유지된다.
- [ ] 백업·가져오기·저장소 정리 기능은 `데이터 도구` 한 진입점에서 찾을 수 있으며 설정 첫 화면에 여러 개의 동급 버튼으로 흩어져 있지 않다.
- [ ] 검사 화면 제목의 설정 버튼을 누르면 검사 설정과 비교 정책이 dialog에서 열리고 결과 목록 아래에는 같은 긴 설정 영역이 반복되지 않는다.
- [ ] 검사 탭에서 로컬/AI 모드 버튼과 AI 연결 프로필·응답 상한을 찾을 수 있고, AI 모드를 켜는 것만으로 전송 미리보기·실행 상태·새 AI 결과가 생기지 않는다.
- [ ] AI 기능을 켠 뒤에도 정적 finding 또는 cluster를 직접 선택하고 전송 미리보기와 호출별 동의를 완료하기 전에는 provider 요청이 없다.
- [ ] 약 430px 폭의 프롬프트 비교에서 변경 종류·대상·내용을 간결한 표로 읽을 수 있고 긴 텍스트 때문에 전체 페이지가 가로로 밀리지 않는다.
- [ ] 700px 이하에서는 패널이 `100vw`·`100dvh`를 채우고 둥근 외곽·resize 손잡이·바깥 여백 없이 앱 셸처럼 표시되며 하단 safe area가 내비게이션을 가리지 않는다.
- [ ] 430px 이하에서 설정·새로고침·닫기 버튼의 터치 영역이 각각 최소 44px이고 상단 노치·safe area와 겹치지 않는다.
- [ ] 모바일에서 헤더를 끌어도 창이 이동하지 않고, 데스크톱에서 저장한 창 크기·위치가 모바일에 적용되지 않는다.
- [ ] 모바일에서 패널을 열고 닫거나 화면 크기를 바꾼 뒤 데스크톱으로 돌아와도 기존 데스크톱 geometry가 모바일 값으로 덮어써지지 않는다.
- [ ] 빠른 시작과 작은 도움말만 제공되며 코치마크·단계별 워크스루가 완성된 것처럼 안내하지 않는다.
- [ ] 위 UI 변경 뒤에도 일반 Chat Completion·Text Completion 요청이 아래 v0.11.4 저장 복구 절차대로 타임라인에 남는다.

## v0.11.4 실제 요청 저장 복구

- [ ] Chat Completion 일반 메시지 한 건이 개인정보 처리 단계에서 `invalid-value` 없이 `저장됨`으로 끝난다.
- [ ] Text Completion 일반 메시지 한 건도 `저장됨`으로 끝난다.
- [ ] 타임라인에 새 요청이 나타나고 패널 새로고침 및 SillyTavern 페이지 새로고침 뒤에도 남는다.
- [ ] 저장 전 캡처 처리 실패를 강제로 재현한 경우 로컬 저장소 오류나 사용할 수 없는 재시도 버튼으로 잘못 표시하지 않는다.

## v0.11.3 캡처 우선 저장

- [ ] 일반 요청 한 건을 보내면 `요청 본문 정리 → 개인정보 보호 설정 적용 → 브라우저 저장소 기록 → 저장된 스냅샷 재확인 → 저장됨` 순서로 끝난다.
- [ ] `저장됨` 직후 타임라인에 스냅샷이 나타나며, 패널 새로고침과 SillyTavern 페이지 새로고침 뒤에도 같은 스냅샷이 남는다.
- [ ] 저장 직후 탐색기에는 최소 final source가 먼저 보일 수 있지만 잠시 뒤 같은 snapshot ID에서 상세 소스 목록으로 보강되고 타임라인 항목 수는 하나만 증가한다.
- [ ] 상세 분석 입력이 매우 큰 테스트에서는 상태가 무한 `처리 중`으로 남지 않고 최소 스냅샷의 `구조화 입력 상세 통계`에 제한 분석 상태가 남는다.
- [ ] 저장 실패 시 상태 문구가 정리·개인정보 처리·저장소 기록·재읽기 검증 중 실패 단계를 구분한다.

## v0.11.2 캡처 종결·간결한 UI

- [ ] 정상 저장 환경에서 AI 의미 검사를 실행하지 않은 일반 요청 한 건을 보내면 10초 안에 `캡처 중 → 처리 중 → 저장됨`으로 끝나고 현재 채팅에 새 스냅샷이 보인다.
- [ ] 토크나이저 응답을 막거나 매우 느리게 만든 테스트 환경에서도 첫 요청은 5초 뒤 로컬 byte/3.35 추정으로 전환해 10초 안에 `저장됨`으로 끝나며, 같은 패널 세션의 다음 요청은 멈춘 tokenizer를 다시 호출하지 않는다.
- [ ] 저장소·개인정보 처리가 실패하는 환경에서는 늦어도 각 30초 제한 뒤 `실패`로 끝나고 `처리 중`에 영구 고정되지 않는다.
- [ ] 메시지가 많은 장문 채팅에서도 AI 의미 검사가 느리게 실행 중이라는 이유만으로 일반 요청의 스냅샷이 누락되지 않는다.
- [ ] AI 의미 검사 자체가 만든 semantic 요청은 `AI 검사 요청 제외`로 구분되고 일반 채팅 스냅샷으로 저장되지 않는다.
- [ ] 캡처 상태나 저장 실패 안내에 프롬프트 원문·snapshot/chat ID·provider/model·원시 오류 메시지가 나타나지 않는다.
- [ ] 패널 폭을 430px 이하로 줄여도 하단의 다섯 기능이 한꺼번에 구분되며 가로 스크롤이나 잘린 글자가 없다.
- [ ] 헤더에는 순서와 접근 가능한 이름이 분명한 `새로고침`·`설정`·`닫기` 세 버튼만 있고 설정 옆 별도 도움말 버튼·모달은 없다.
- [ ] 첫 스냅샷이 없는 채팅에서 빠른 시작 단계가 나타나며, 각 제목·필드의 짧은 설명은 PC hover·키보드 focus·모바일 click tooltip으로 확인할 수 있다.
- [ ] 규칙 검사 진입 직후 선택한 로컬 또는 AI 모드의 결과가 먼저 보이고, 검사 설정·비교 정책은 제목의 설정 버튼을 누르기 전 대량 내용을 렌더링하지 않는다.
- [ ] 검색 필터, 타임라인 저장 관리와 설정의 고급 항목이 접혀 있으며 disclosure 제목이 모두 좌측 정렬되고 필요할 때 각각 펼칠 수 있다.

## 기본 OFF·개인정보 모드

- [ ] 업데이트 직후 규칙 검사 화면의 AI 모드가 꺼져 있고 AI 호출을 바로 실행할 수 없다.
- [ ] AI 설정이 꺼져 있어도 기존 정적 Rule Inspector finding·클러스터·검토 판정은 정상 표시된다.
- [ ] 규칙 검사에서 AI 모드를 켜고 응답 상한을 저장한 뒤에만 선택 UI가 나타나며 처음에는 finding·cluster가 하나도 체크되지 않는다.
- [ ] `원문 제거본`과 `메타데이터만` 스냅샷에서는 full 원문을 복원하거나 AI 준비를 시도하지 않고 전체 원문 전용 안내를 표시한다.
- [ ] 개인정보 모드 메타데이터가 없던 구버전 기록을 v7로 이전해도 AI 검사가 원문 보존 상태를 추측하지 않고 차단되며, 업데이트 뒤 `전체 원문` 모드에서 새로 만든 테스트 스냅샷은 선택할 수 있다.
- [ ] full 스냅샷으로 돌아오면 사용자가 직접 대상을 선택할 수 있지만 이전 개인정보 모드의 선택·결과를 재사용하지 않는다.

## AI 연결 프로필

- [ ] 규칙 검사 AI 모드의 `AI 연결 프로필`에 `현재 채팅 연결`과 SillyTavern Connection Manager가 공개한 지원 프로필 이름만 표시되고 API 키·URL·비밀번호·proxy 값은 나타나지 않는다.
- [ ] 테스트용 프로필을 선택해 설정을 적용하고 패널을 닫았다 다시 열어도 같은 프로필 이름이 선택되어 있다.
- [ ] 프로필 목록 조회가 일시적으로 실패한 상태에서 다른 설정만 적용해도 기존 프로필 선택이 지워지지 않고 opaque ID가 화면 문구에 노출되지 않는다.
- [ ] 설정 저장값에는 선택한 opaque profile ID만 있고 profile 객체·API key·URL·연결 비밀값이 복제되어 있지 않다.
- [ ] 선택한 Chat Completion 또는 Text Completion 프로필로 미리보기를 열면 해당 프로필의 provider/model identity가 표시되고 현재 채팅 연결을 변경하지 않은 채 그 프로필로만 요청한다.
- [ ] 미리보기 뒤 같은 provider/model의 다른 프로필로 바꾸면 기존 동의나 cache를 재사용하지 않고 새 미리보기를 요구한다.
- [ ] 프로필 공개 API를 지원하지 않거나 저장한 프로필을 SillyTavern에서 삭제한 뒤 다시 열면 현재 채팅 연결을 사용하며 사라진 프로필을 추측해 복원하지 않는다.
- [ ] 선택한 프로필 요청을 의도적으로 실패시키면 같은 AI 검사에서 현재 채팅 연결로 두 번째 요청을 보내지 않고 한 번의 실패로 끝난다.

## 대상 선택·정확한 전송 미리보기

- [ ] 정적 finding 또는 지시 cluster 하나를 체크하기 전에는 `전송 내용 미리보기` 버튼이 비활성화되어 있다.
- [ ] 대상을 체크하면 선택 수가 맞고, 다른 스냅샷으로 바꾸거나 해당 finding이 사라지면 끊어진 선택이 자동으로 제거된다.
- [ ] 미리보기의 `전송할 소스` 이름과 원문이 선택 대상의 실제 source card와 문자 단위로 같으며 요약본·일부 preview·최종 전체 프롬프트로 바뀌지 않는다.
- [ ] closure 밖 source, 비활성·실제 요청 미포함·대안 제외 source가 전송 목록에 섞이지 않고 제외 목록에 이유와 함께 표시된다.
- [ ] 현재 SillyTavern 연결에서 확인한 provider/model이 표시된다. model을 확인하지 못한 경우 임의 model·비용을 만들지 않고 미확인 상태를 그대로 표시한다.
- [ ] 입력 토큰은 예상치, 응답 토큰은 설정한 상한으로 구분되고 상한이 비용 상한이라고 설명하지 않는다.
- [ ] provider가 응답에 통화와 비용을 명시해 저장한 경우에만 비용을 표시하고, 그 밖에는 로컬 단가로 추측하지 않는다.

## 호출별 동의·취소·재시도

- [ ] 미리보기를 열 때마다 동의 체크가 선택 해제되어 있고 체크하기 전에는 실행 확인 버튼이 비활성화되어 있다.
- [ ] 모달의 취소·닫기·바깥 영역·Escape로 나가면 provider 호출이나 AI 결과가 생기지 않고 원래 버튼으로 focus가 돌아온다.
- [ ] 다시 미리보기를 열거나 오류 뒤 `새 미리보기로 다시 시도`를 누르면 이전 동의를 기억하지 않고 선택한 프로필 또는 현재 연결의 provider/model·원문으로 다시 준비한다.
- [ ] 동의 뒤 실행 중 `검사 취소`를 누르면 늦게 도착한 결과가 화면·cache에 반영되지 않는다.
- [ ] 취소 안내가 이미 시작된 provider 계산·전송·과금까지 물리적으로 중단했다고 주장하지 않는다.
- [ ] 미리보기를 연 뒤 SillyTavern의 provider/model을 바꾼 경우 이전 준비를 다른 연결로 보내지 않고 identity 변경 오류로 실패한다.

## AI 제안·정적 결과 불변

- [ ] 정상 응답은 `AI 제안 — 자동 적용되지 않음`이라는 별도 영역에 제목·요약·판단 이유·신뢰도와 접힌 검증 근거로 표시된다.
- [ ] AI 제안을 받아도 기존 정적 finding 수·심각도, 유효/오탐 판정, 항상 무시, 비교 정책과 audit log가 바뀌지 않는다.
- [ ] AI 결과 카드에는 자동 수정·판정 적용·정책 저장 버튼이 없고 SillyTavern 원본 프롬프트·캐릭터·생성 설정도 바뀌지 않는다.
- [ ] 잘못된 JSON, 알 수 없는 source/atom/relation ID 또는 원문과 다른 quote를 반환하는 provider 응답은 일부 카드로 표시하지 않고 전체 오류로 처리한다.
- [ ] 스냅샷·선택 대상을 바꾸면 이전 선택과 맞지 않는 AI 결과가 남지 않고, 새로고침하면 선택·AI 결과·memory cache가 사라진다.
- [ ] AI가 title·summary·rationale에 입력 원문 표현을 반복할 수 있으므로 메모리 cache를 익명화·비밀 제거 기능으로 오해하지 않는다.

## 자기 캡처·동시 generation

- [ ] AI 검사를 한 번 실행해도 그 semantic prompt 자체가 타임라인의 일반 채팅 스냅샷으로 추가되지 않는다.
- [ ] 가능하면 AI 검사와 일반 사용자 generation을 겹쳐 실행해 일반 generation의 snapshot·lore·lifecycle이 사라지거나 AI 내용과 섞이지 않는지 확인한다.
- [ ] 식별자 없는 동시 요청을 구분할 수 없는 상황에서는 다른 pending generation을 임의 소비하지 않고 미연결·실패 상태로 닫힌다.
- [ ] 명시적인 공개 ID로 연결되는 정상 사용자 generation은 AI 검사와 겹쳐도 정확한 자기 snapshot에 연결된다.
- [ ] 취소·timeout 직후 재시도하면 오래된 nonce나 늦은 AI 응답이 새 실행의 결과·snapshot으로 재사용되지 않는다.

## 단일 generation lifecycle

- [ ] 새 메시지를 한 번 생성하면 스냅샷의 생성 상태가 `시작 → 종료` 흐름과 맞고 같은 snapshot ID가 유지된다.
- [ ] 생성 도중 중단하면 해당 스냅샷만 `중단`으로 바뀌고 직전 스냅샷의 lifecycle은 변하지 않는다.
- [ ] request-ready 이벤트가 있는 경로에서는 요청 캡처 상태와 사용한 이벤트가 실제 생성 경로와 맞다.
- [ ] prompt-only 대체 경로에서는 provider 요청 본문을 확보했다고 표시하지 않고 대체 캡처임을 설명한다.
- [ ] OpenRouter·proxy·사용자 지정 API의 내부 upstream provider는 공개 근거 없이 업체명으로 확정되지 않는다.

## 로컬 입력·출력 usage

- [ ] 새 스냅샷의 입력 토큰은 `local-estimate`와 `local-prompt-tokenizer` 출처로 표시되고 출력·캐시·합계는 근거가 생기기 전 `알 수 없음`으로 남는다.
- [ ] `MESSAGE_RECEIVED` 뒤 현재 메시지의 `extra.token_count`가 유효하고 하나의 활성 generation만 선택 가능한 경우 출력 토큰이 로컬 추정치로 합쳐진다.
- [ ] 출력 토큰이 합쳐져도 provider 보고값·청구 금액·캐시 토큰으로 잘못 표시되지 않는다.
- [ ] 메시지에 유효한 `extra.token_count`가 없으면 출력값을 임의 계산하지 않고 `산정 불가`로 남는다.
- [ ] UI의 capability matrix에서 공식 response event·stream usage·public response correlation·provider 비용이 기본적으로 `지원 안 함`으로 표시된다.
- [ ] 별도 integration을 설치하지 않은 기본 SillyTavern에서 provider-reported usage가 나타나면 원문이나 API key 없이 재현 정보를 기록한다.

## 동시 generation과 실패로 닫히는 연결

- [ ] 가능하다면 두 generation을 겹쳐 실행해 각 스냅샷의 생성 유형·로어북·중단/종료 상태가 서로 바뀌지 않는지 확인한다.
- [ ] 동시 generation 중 `MESSAGE_RECEIVED`만으로 대상을 하나로 고를 수 없으면 출력 토큰이 어느 스냅샷에도 임의 연결되지 않는다.
- [ ] 공개 ID가 없는 prompt→request 호환 연결은 동작하더라도 response usage를 FIFO로 붙이지 않는다.
- [ ] 공개 ID가 충돌하거나 한쪽 이벤트에만 있는 경우 다른 pending generation으로 fallback하지 않고 미연결·산정 불가 상태를 유지한다.
- [ ] 동시 생성 종료 뒤 새 단일 generation을 만들면 오래된 session의 lore·usage·lifecycle이 새 스냅샷에 재사용되지 않는다.

## 비용 표시 경계

- [ ] 설정에 사용자 가격표 입력·추가·삭제 UI가 없다.
- [ ] 예전 버전에서 저장한 가격표가 남아 있어도 스냅샷 비용을 다시 계산하지 않는다.
- [ ] provider가 직접 보고한 통화 포함 비용은 그대로 표시하고 그 밖에는 `계산 불가`로 남긴다.
- [ ] 설정 어디에도 OpenAI·Anthropic·Google 등의 최신 가격이 자동 입력되거나 내장 가격표로 갱신되지 않는다.

## 스키마 v7·개인정보·archive 이전

- [ ] 업데이트 직후 대표적인 v0.10.0 이하 스냅샷이 원문·snapshot ID·시간을 유지한 채 열린다.
- [ ] 구버전 `stats.totalTokens`는 입력 전용 `legacy-snapshot-token-count` 로컬 추정치로 보이고 출력·캐시·provider 비용은 추측되지 않는다.
- [ ] 새 스냅샷과 이전된 스냅샷을 JSON으로 검토할 때 `capture.correlationId`·`request.correlationId`에 raw ID가 남지 않고 `hadCorrelationId` boolean만 남는다.
- [ ] 요청 본문의 알려진 root·metadata correlation key는 제거되지만 프롬프트 도메인 데이터의 일반 `id`는 사라지지 않는다.
- [ ] full·redacted·metadata 스냅샷이 섞인 기존 archive를 테스트 프로필에 병합하면 privacy mode와 usage 상태가 유지된다.
- [ ] 같은 archive를 다시 병합하면 중복 항목이 늘지 않고, 전체 교체는 정확한 확인 문자열 없이는 시작되지 않는다.
- [ ] archive 가져오기 또는 v7 이전에 실패한 항목은 성공으로 표시되지 않으며 정상 형제 스냅샷은 계속 열린다.
- [ ] redacted·metadata 공유 문서에 raw correlation ID, 요청 원문, 가격 설정 전체가 새로 포함되지 않는다.

## 원자적 후속 갱신과 저장 실패

- [ ] generation 종료 또는 출력 usage가 캡처 저장보다 늦게 도착해도 같은 snapshot ID의 lifecycle·usage만 갱신된다.
- [ ] 후속 갱신 뒤 타임라인 정렬, 전체 스냅샷 수와 대략적 용량이 새로고침 전후에 일치한다.
- [ ] 저장 실패가 자연스럽게 발생하면 다른 snapshot으로 갱신을 옮기지 않고 실패 상태를 표시한다.
- [ ] 재시도 시 새 snapshot을 중복 생성하지 않고 기존 저장 identity를 유지한다.

## 모바일·좁은 화면 UI

- [ ] SillyTavern의 밝은 테마·어두운 테마와 ST DevTools의 자동·밝게·어둡게 모드에서 헤더 세 버튼, disclosure 제목, 일반 버튼과 select가 같은 방향·폭으로 표시된다.
- [ ] 약 430px 폭에서 SillyTavern 테마가 `.menu_button`에 전체 폭·세로 writing-mode·잘못된 flex를 강제하지 않고 헤더 세 버튼과 패널 조작이 한 글자씩 쪼개지지 않는다.
- [ ] 약 430px 폭에서 일반 설정에 사용자 가격표 항목이 없고 보관·개인정보 입력이 가로로 잘리거나 패널 밖으로 나가지 않는다.
- [ ] AI 대상 선택·실행·취소 버튼과 긴 source 이름·결과 제목이 한 글자씩 세로로 쪼개지거나 패널 밖으로 나가지 않는다.
- [ ] AI 동의 모달의 provider/model 표, 전체 source 원문, 제외 이유, 동의 체크와 취소·실행 버튼을 가로 스크롤 없이 확인할 수 있다.
- [ ] AI 동의 모달에서 Tab·Shift+Tab focus가 모달 밖으로 빠지지 않고 Escape 뒤 원래 실행 버튼으로 돌아온다.
- [ ] 컨텍스트 usage 카드의 네 토큰 항목, 상태, 비용·출처·기준일이 겹치지 않고 긴 model/provider 이름이 안전하게 줄바꿈된다.
- [ ] capability matrix를 펼치고 닫을 수 있으며 도움말이 화면 경계를 벗어나지 않는다.
- [ ] 설정 저장 오류 뒤 적용 버튼이 영구 비활성화되지 않고 수정·취소·재진입이 가능하다.
- [ ] 키보드 focus가 적용·취소와 usage capability 토글에서 보인다.

## 문제 보고 시 남길 정보

- [ ] SillyTavern·ST DevTools·브라우저 버전과 일반/비공개 창 여부를 기록한다.
- [ ] 단일/동시 generation, Chat/Text Completion, 선택 source·model과 lifecycle 순서를 기록한다.
- [ ] usage 문제는 상태, 화면에 표시된 source event, 입력·출력 값의 존재 여부만 기록한다.
- [ ] 비용 문제는 provider 보고값 존재 여부와 통화 코드만 기록하고 실제 금액이나 응답 원문은 첨부하지 않는다.
- [ ] AI 문제는 기본 설정 상태, 현재 연결/선택 프로필 중 사용한 경로, 선택 target 종류·개수, full/redacted/metadata, preview identity 상태와 안정된 `SEMANTIC_*` 오류 code만 기록한다. API key·URL·opaque profile ID는 기록하지 않는다.
- [ ] 이전·복원 문제는 원본 snapshot schema, privacy mode, merge/replace와 오류 code만 기록한다.
- [ ] prompt 원문·AI preview/response·API key·raw request/response ID·전체 archive·IndexedDB raw 값은 이슈에 첨부하지 않는다.

## 다음 버전 방향

다음 단계는 실제 초보자 검토에서 확인되는 briefing 길이·practice task·debrief 결과 설명과 320px·390px auto-height sheet 밀도를 v0.15.x에서 보정하고, 구조 제품 경로에서 아직 생성되지 않는 말투·안전 atom 분류를 오탐 없이 보강하는 것입니다. 실제 provider 호환 문제는 재현 가능한 bounded fixture가 생길 때 패치합니다.
