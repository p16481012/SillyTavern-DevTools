{
  "name": "sillytavern-devtools",
  "version": "0.8.4",
  "private": true,
  "type": "module",
  "description": "Read-only prompt pipeline diagnostics for SillyTavern.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "sandbox": "node scripts/manual-ui-server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-or-later"
}
