# 변경 기록

## 0.12.2 - 2026-08-01

### 다섯 기능 하단 내비게이션

- 빈 값과 중복 정보가 많던 `요청 상세` 탭을 제거하고 하단 기능을 `전송 프롬프트`·`기록`·`변경 비교`·`규칙 검사`·`검색` 다섯 개로 단순화
- 규칙 검사를 검색 바로 왼쪽으로 옮기고 선택 상태·roving `tabindex`·좌우 화살표·Home·End 이동 순서를 같은 배열로 통일
- 요청 상세에서 유지할 `생성 설정`과 `프롬프트 payload`를 전송 프롬프트 화면 아래의 접힌 요청 원본 영역으로 이동하고 캡처·개인정보 저장 형식은 변경하지 않음

### 프롬프트 탐색과 이동 보강

- 프리셋 프롬프트 그룹을 기본 접힘으로 변경하고 전체 프리셋과 이번 요청에 실제 포함된 프롬프트만 보는 `요청 포함만` 전환 기능 추가
- 최종 프롬프트가 아직 lazy mount되지 않은 접힌 상태에서도 `최종 위치 보기`가 그룹과 카드를 먼저 열고 해당 문자 범위로 이동하도록 수정
- 연결 방식 오른쪽의 실제 버튼으로 최종 위치 이동을 배치하고 소스 카드와 최종 프롬프트 범위에 대응하는 식별 색을 적용
- 구조 위치 도움말이 닫힌 카드의 overflow 경계에 잘려 작은 스크롤 상자로 표시되던 문제와 카드 배경이 둥근 모서리 밖으로 튀어나오던 문제 수정

### 설정과 검사 흐름 정리

- 패널 테마는 선택 즉시 저장해 하단 적용 버튼까지 스크롤하지 않아도 유지되도록 변경
- AI 의미 검사 사용 여부·연결 프로필·응답 상한을 일반 설정에서 규칙 검사 화면의 AI 모드로 이동하고, AI 모드만 켜서는 provider 요청이 발생하지 않는 기존 미리보기·호출별 동의 경계를 유지
- 규칙 검사 설정과 비교 정책은 제목의 설정 버튼에서 여는 전용 패널로 이동해 결과 목록 아래의 긴 설정 영역을 제거

## 0.12.1 - 2026-08-01

### 여섯 기능 단층 내비게이션 교정

- v0.12.0의 `프롬프트`·`검사`·`기록`·`도구` 4개 그룹과 조건부 하위 탭을 폐기하고 `전송 프롬프트`·`규칙 검사`·`기록`·`변경 비교`·`요청 상세`·`검색`을 고정 하단 내비게이션에서 직접 선택하도록 변경
- 마지막으로 열었던 하위 탭에 따라 그룹 버튼의 목적지가 달라지던 상태를 제거하고, 여섯 기능의 순서·아이콘·선택 상태와 좌우 화살표·Home·End 키 이동을 하나의 tablist로 통일
- 중앙 콘텐츠만 스크롤하며 하단 내비게이션은 항상 보이도록 패널을 `헤더 / 콘텐츠 / 내비게이션` 앱 셸로 단순화
- 각 화면에 실제 기능 이름 제목을 추가하고 짧은 설명을 제목 옆 tooltip에서 PC hover·키보드 focus·모바일 click으로 확인하도록 구성
- `전송 프롬프트` 첫 화면에 총 토큰·컨텍스트 사용률·남은 컨텍스트·provider/model·현재 요청 선택을 한 장에 모은 요청 요약 카드를 추가

### 모바일 앱 셸·geometry 경계

- 700px 이하에서 패널을 `100vw`·`100dvh`로 채우고 하단 safe area를 포함해 여섯 기능을 한 줄에서 바로 선택하도록 배치
- 430px 이하 헤더의 설정·새로고침·닫기 터치 영역을 44px로 유지하고 상단 safe area를 반영
- 뒤에 선언된 호스트 테마의 `!important` 입력·선택창 규칙보다 패널 색 토큰이 우선하도록 격리해 어두운 모드의 흰 배경·흰 글자 충돌을 방지
- 모바일에서는 데스크톱 geometry를 복원하지 않고 크기·위치 변경을 저장하지 않으며 헤더 pointer drag를 시작하지 않도록 분리. 데스크톱의 위치 저장·크기 조절·드래그는 유지
- 설정 모달, 캡처·저장 파이프라인, 개인정보 모드, Rule Inspector와 선택적 AI의 대상 선택·전송 미리보기·호출별 동의·no-send 기본 경계는 변경하지 않음

## 0.12.0 - 2026-08-01

### 처음 사용자를 위한 작업 중심 UI

- 각 화면에서 가장 중요한 행동을 먼저 보여 주는 토스식 `한 화면·한 우선 행동` 정보 구조 적용
- `프롬프트`·`검사`·`기록`·`도구` 네 작업 탭에 구분 아이콘을 추가하고, 헤더 이름 옆 상태 점으로 캡처 대기·처리·저장·실패 상태를 간결하게 표시
- 카드·disclosure 요약 제목을 좌측 정렬하고 도움말 아이콘의 시각 크기를 줄여 설명이 본문보다 먼저 눈에 띄지 않도록 조정
- 프롬프트 비교의 변경 내역을 좁은 모바일 화면에서도 읽을 수 있는 간결한 표 배치로 보강

### 테마·설정·검사 흐름

- 패널 내부에 고대비 밝은/어두운 색 토큰을 정의하고 SillyTavern 호스트 테마의 광범위한 button·select·table·상태색 규칙이 내부 UI를 덮어쓰지 않도록 격리 강화
- 설정 모달을 열 때 첫 입력칸에 자동 포커스하지 않고 패널에 포커스를 두며, 상단 닫기 버튼을 유지하고 자주 쓰는 설정은 펼친 상태로 표시
- 백업·가져오기·저장소 정리 등 데이터 관리 기능을 `데이터 도구` 한 진입점으로 모아 초기 설정 화면의 선택 수와 스크롤 부담 축소
- 검사 탭 안에 선택적 AI 의미 검사 opt-in을 배치. 기능 활성화는 설정만 저장하며 provider 요청을 보내지 않고, 실제 호출은 기존처럼 대상 선택·전송 미리보기·호출별 동의를 모두 통과해야 함

### 범위와 검증 경계

- 캡처·저장·Rule Inspector·AI 엄격 검증·읽기 전용 원본 경계는 변경하지 않고 UI 표현과 탐색 계층을 중심으로 정리
- 빠른 시작과 필드별 도움말은 유지하지만 완성된 온보딩으로 간주하지 않음. 코치마크·워크스루는 실사용 피드백 뒤 별도 버전에서 설계

## 0.11.4 - 2026-08-01

### 실제 SillyTavern 선택 필드 저장 복구

- 요청 sanitizer의 느슨한 null 비교 때문에 `undefined`가 그대로 남던 결함을 수정. OpenAI·Text Completion 요청의 선택적 생성 필드와 메시지 `name`처럼 SillyTavern이 정상적으로 `undefined`를 넣는 값은 저장 전 `null`로 정규화
- sparse array hole도 dense한 `null`로 바꾸고, 재정의된 Date 메서드를 실행하지 않은 채 표준 Date 값만 ISO 문자열로 변환해 privacy `invalid-value` 재발 방지
- 실제 OpenAI·TextGen 형태의 선택 필드, 개인정보 변환, `SnapshotStore` 기록과 저장 직후 재조회까지 연결한 회귀 테스트 추가
- 개인정보 변환 전에 실패한 오류를 로컬 저장소 오류로 잘못 표시하거나 존재하지 않는 재시도를 안내하지 않고, 캡처 처리 단계와 안정적인 오류 코드를 설명하도록 UI 수정

### 검증

- 명시적 `undefined`, sparse array, OpenAI·TextGen 선택 필드를 포함한 요청이 세 개인정보 모드와 persistent backend 재접속에서 저장되는지 검증

## 0.11.3 - 2026-07-31

### 캡처 우선 저장 복구

- 소스 귀속·토크나이저 계산이 끝나기 전에 정확한 요청 payload와 최종 프롬프트를 가진 최소 스냅샷을 먼저 저장하고 UI에 전달하도록 캡처 순서를 변경
- 저장 직후 동일 채팅 partition과 snapshot ID로 레코드를 다시 읽어 IndexedDB 기록과 타임라인 index를 검증하며, `정리`·`개인정보 처리`·`저장소 기록`·`재읽기 검증` 단계를 상태 표시에서 구분
- 최소 스냅샷이 저장된 뒤 상세 소스 귀속과 tokenizer 계산을 유휴 작업으로 실행하고 같은 ID의 레코드를 원자적으로 보강해, 상세 분석이 느리거나 실패해도 검토 가능한 원문 기록은 유지
- 비활성 프롬프트가 지나치게 많거나 분석 입력이 큰 경우 상세 분석을 제한하고 최소 스냅샷을 유지하는 작업량 상한 추가
- getter 예외, 함수·Symbol·BigInt·비정상 숫자·typed array처럼 IndexedDB structured clone을 깨뜨릴 수 있는 값을 캡처 경계에서 JSON 안전 값으로 정규화

### 검증

- 실제 `SnapshotStore`로 최소 선저장·재읽기·동일 ID 상세 보강을 검증하고, 세 개인정보 모드의 persistent backend 재접속과 401개 비활성 프롬프트·종료되지 않는 tokenizer에서도 영구 스냅샷이 먼저 남는 회귀 테스트 추가

## 0.11.2 - 2026-07-31

### 캡처 영구 대기 방지

- SillyTavern prompt tokenizer의 Promise가 끝나지 않으면 5초 뒤 UTF-8 byte/3.35 로컬 추정으로 전환하고, 같은 패널 세션에서는 멈춘 tokenizer를 다시 호출하지 않도록 최초 probe로 가용성 판정
- 개인정보 변환과 스냅샷 저장 작업에 각각 30초 제한을 적용하고, 저장 전 예외·동기 callback 실패·저장소 timeout을 포함한 모든 캡처 경로가 개인정보 없는 `saved` 또는 `failed` 상태로 종결되도록 보강
- tokenizer·개인정보 처리·저장소의 미종료 Promise와 동시 캡처를 재현해 저장 성공 또는 실패 종결과 상태 detail의 원문 비노출을 검증하는 회귀 테스트 추가
- 요청 준비가 실패해도 generation ledger의 prompt와 fallback timer를 함께 종료해 다음 요청 상관관계와 세션 정리를 오염시키지 않도록 보강

### 간결한 헤더와 테마 방어

- 설정 옆 헤더 도움말 버튼과 별도 도움말 모달을 제거하고 새로고침·설정·닫기 세 버튼만 유지. 필드별 도움말과 첫 스냅샷 전 빠른 시작은 계속 제공
- disclosure 제목을 좌측 정렬로 통일하고 기본 펼침 표시자는 유지하며, SillyTavern 전역 테마가 패널 안 `button`·`select`·`.menu_button`의 폭·flex·writing-mode를 덮어쓰지 못하도록 패널 범위 스타일 방어
- 여러 테마와 430px 화면에서 헤더·disclosure·버튼 배치를 확인하는 UI 회귀 검증 보강

### AI 연결 프로필 선택

- SillyTavern Connection Manager 공개 서비스가 제공하는 지원 프로필을 이름으로 선택해 AI 의미 검사에 사용할 수 있는 설정 추가
- 설정 V1~V4를 V5로 이전하고 ST DevTools에는 최대 256자의 불투명 profile ID만 저장. API 키·URL·비밀번호·proxy 등 연결 비밀값은 읽거나 저장하지 않도록 프로필 목록을 제한된 표시 필드로 정제
- 공개 프로필 API가 미지원이거나 저장한 프로필이 사라진 경우 요청 전에 현재 채팅 연결을 사용하고, 선택한 프로필 요청이 시작된 뒤 실패하면 현재 연결로 자동 재시도하지 않아 이중 호출·과금 방지
- Text Completion 프로필에는 동의 미리보기의 semantic prompt 문자열을 그대로 넘기고 별도 instruct 재구성을 끄며, 연결 프로필의 sampler preset만 적용해 미리보기·토큰 추정·자가 캡처 경계를 일치
- 미리보기 identity와 cache digest에 현재 연결/프로필 경로 및 opaque profile ID를 결속해 같은 provider/model의 다른 프로필로 바뀌어도 전송 전 실패하고 결과 cache가 섞이지 않도록 보강. 프로필 목록을 일시적으로 읽지 못한 설정 화면은 저장 ID를 노출하지 않은 채 선택을 보존
- Chat Completion·Text Completion 프로필, 목록 미지원·프로필 소실·프로필 요청 실패와 현재 연결 무재시도 경계를 자동 검증

## 0.11.1 - 2026-07-31

### 캡처 회귀 수정과 상태 안내

- 느린 AI 의미 검사 호출의 semantic gate가 활성인 동안 긴 일반 채팅이 기존 256개 문자열 탐색 한계를 넘어가면 prompt 단계에서 누락되던 회귀 수정
- semantic gate 탐색을 최대 2MiB·8,192개 노드·4,096개 문자열로 제한한 채 일반적인 장문 채팅을 판별하며, 이 한계도 넘는 입력은 AI 원문 유출을 막기 위해 계속 fail-closed
- 캡처 원문·스냅샷/채팅 ID·provider/model·오류 메시지 없이 `capturing`·`processing`·`saved`·`failed`·`excluded-semantic`·`skipped-safety`만 알리는 `capture-status` 이벤트 추가
- 일반 사용자 요청은 저장 흐름을 표시하고, AI Semantic Inspector가 자체 생성한 요청은 의도적으로 스냅샷에서 제외되었음을 구분

### 초보자 중심 UI 정리

- 기존 6개 탭을 `프롬프트`·`검사`·`기록`·`도구`의 4개 작업 중심 1차 탐색과 각 작업의 세부 기능으로 재구성
- 첫 스냅샷 전 빠른 시작 단계와 상단 도움말을 추가하고, 캡처 진행·저장·실패·의도적 AI 제외 상태를 짧게 표시
- 규칙 검사는 결과를 설정보다 먼저 보여 주고 비교 정책·AI 의미 검사 같은 고급 영역은 펼칠 때만 내용을 생성
- 검색 필터, 타임라인 저장 관리와 설정의 고급 항목을 접을 수 있게 하여 초기 화면의 설명과 스크롤 부담 축소
- 430px 모바일에서도 1차 탐색·세부 탐색·도움말·상태 표시가 가로로 넘치지 않도록 반응형 배치 보강

### 검증

- 긴 일반 요청 저장, exact AI 요청 제외, bounded 상태 detail과 실패·재시도 경로를 자동 회귀 검증
- 작업 중심 탐색, 빠른 시작·도움말, 규칙 결과 우선, 검색·타임라인·설정 disclosure의 UI 계약 검증

## 0.11.0 - 2026-07-31

### 선택적 AI 의미 검사

- 기본값이 꺼진 `AI 의미 검사` 설정과 64~2,048 범위의 응답 토큰 상한 추가
- 정적 finding·지시 클러스터를 사용자가 직접 고르기 전에는 실행 버튼이 활성화되지 않고 아무 대상도 자동 선택하지 않도록 구현
- 매 호출 전에 선택 대상 closure의 정확한 전체 원문, 제외 소스와 이유, 현재 provider/model identity, 예상 입력 토큰, 응답 상한과 정확히 일치한 사용자 가격 override 비용을 표시하는 전용 미리보기 추가
- 미리보기마다 선택 해제로 초기화되는 1회 동의 체크를 추가하고, 취소 시 provider 호출 0회·재시도 시 새 준비/미리보기/동의를 보장
- `원문 제거본`·`메타데이터만` 스냅샷은 준비 전에 차단하고 전체 원문 스냅샷만 허용
- 개인정보 메타데이터 없이 v7로 이전된 구버전 기록도 과거 원문 보존 상태를 추측하지 않고 차단하며 새 전체 원문 스냅샷 재캡처 안내

### 요청·응답 안전 경계

- 선택 finding·cluster에서 실제로 연결된 source·atom·relation만 closure로 구성하고 비활성·미포함·대안 제외·대화/최종 소스를 제외 이유와 함께 분리
- 필수 source에 민감 토큰이 있으면 offset을 바꾸는 부분 정제 대신 요청 전체를 실패로 닫도록 구현
- prompt 원문을 비신뢰 인용 데이터로 선언하고 버전·필드·크기·깊이·노드 수가 제한된 JSON 한 개만 허용
- 알 수 없는 target/source/atom/relation ID, 추가 필드, Markdown wrapper, 잘못된 evidence 범위 또는 실제 source substring과 다른 quote가 하나라도 있으면 응답 전체를 폐기
- 검증된 결과를 `AI 제안 — 자동 적용되지 않음` 영역에만 표시하고 정적 finding·검토 판정·비교 정책·항상 무시·SillyTavern 원본을 변경하지 않도록 분리

### Provider adapter·캡처 억제·메모리 경계

- SillyTavern 공개 `getContext().generateRaw()`만 호출하는 provider adapter와 현재 provider/model identity의 준비·전송 직전 재확인 추가
- identity가 준비 뒤 바뀌면 실패로 닫고, provider만 확인되는 partial 상태는 모델·비용을 추측하지 않은 채 미리보기에 그대로 표시
- AI prompt와 prompt 유형이 정확히 일치하는 nonce ticket 호출 및 exact duplicate만 자기 캡처에서 제외하고 TTL·용량·identity 소비 규칙을 적용
- 동시에 식별자 없는 사용자 요청과 AI 요청을 안전하게 구분하지 못하면 어느 pending 요청에도 추측 연결하지 않는 fail-closed 경로 추가
- 메모리 전용 digest cache는 전체 raw prompt·전체 raw provider response와 전용 source content·evidence quote 필드를 저장하지 않고 검증된 offset의 quote를 현재 준비 원문에서 다시 구성. 다만 title·summary·rationale 같은 정규화 제안 텍스트가 모델이 반복한 원문 표현을 포함할 가능성은 있음
- 취소·시간 제한 뒤 늦은 provider 결과를 폐기하는 논리적 취소를 적용. 이미 시작된 provider 계산 자체를 강제로 중단하거나 과금을 되돌리지는 않음

### UI·샌드박스·검증

- AI 선택·결과 상태와 정적 검사 컨트롤을 분리하고 선택 변경·사라진 대상에 남은 AI 결과를 폐기
- 전용 동의 모달에 Escape, 포커스 트랩·복원, 430px 이하 원문/버튼 오버플로 방어 적용
- 실제 `SemanticInspector` 준비·엄격 검증·메모리 cache에 결정적 fake adapter만 주입하는 성공·오류·취소 샌드박스 추가. 실제 provider·네트워크 호출은 0회
- 기본 OFF·설정 V3→V4 이전, full-only 준비, 동의 취소 no-send, retry 재동의, logical cancel, strict response/evidence, self-capture·동시성 경계를 자동 회귀 검증
- 전체 자동 테스트 483개와 JavaScript 파일 104개의 문법 검사 통과

## 0.10.1 - 2026-07-31

### Generation 상관관계와 lifecycle

- pending prompt·request·lore·lifecycle·usage를 generation별 불투명 handle에 격리하는 크기·수명 제한 session ledger 추가
- prompt와 request 양쪽에 같은 공개 ID가 있을 때만 정확히 연결하고, 같은 ID 충돌·중복 객체·오래된 session은 실패로 닫히도록 변경
- 공개 ID가 없는 호환 경로는 prompt→request 연결에만 유형별 FIFO를 사용하고 response usage에는 FIFO를 사용하지 않도록 경계 고정
- `GENERATION_STARTED`·`GENERATION_STOPPED`·`GENERATION_ENDED`와 중첩 로어북 상태가 동시에 진행되는 generation 사이에서 섞이지 않도록 분리
- lifecycle과 usage 후속 반영을 스냅샷 ID·저장 partition을 바꿀 수 없는 원자적 `SnapshotStore.updateSnapshot()` 경로로 갱신

### Usage·비용과 공식 이벤트 경계

- OpenAI·Anthropic·Google·호환 응답 형식의 토큰 수를 제한된 정규형으로 변환하는 parser와 음수·NaN·과대·모순값·prototype 오염 거부 추가
- `provider-reported`·`local-estimate`·`unlinked`·`unavailable` usage, 입력·출력·캐시·합계 토큰, 근거 이벤트·연결 시각을 분리한 컨텍스트 카드 추가
- 새 스냅샷에 로컬 prompt tokenizer 입력 추정치를 기록하고, 유효한 `MESSAGE_RECEIVED`의 `extra.token_count`를 하나의 활성 generation만 고를 수 있을 때 출력 추정치로 병합
- 공식 SillyTavern 공개 이벤트에는 provider response usage와 대응 provider request ID가 없음을 capability matrix와 UI 설명에 명시
- 별도 integration이 같은 공개 ID를 제공할 때만 provider usage를 연결할 수 있는 adapter를 마련하되 기본 설치에서 provider usage를 추측하지 않음
- provider가 직접 보고한 비용 또는 사용자가 입력한 provider·model·currency 정확 일치 단가만 계산하고 내장 가격표·통화 자동 선택·유사 model 추측을 금지

### 스키마 v7과 개인정보

- 스냅샷 스키마를 v7로 올리고 정규화된 usage와 비용 출처를 추가
- v1~v6 지연 이전에서 구버전 `stats.totalTokens`만 입력 전용 `legacy-snapshot-token-count` 로컬 추정치로 보존하고 출력·캐시·provider 비용은 산정 불가로 유지
- 저장된 `capture.correlationId`·`request.correlationId`와 요청 본문의 알려진 상관관계 키를 제거하고 `hadCorrelationId` boolean만 보존
- 일반 도메인의 `id`와 상관관계 컨테이너 밖의 동명 필드는 제거하지 않는 제한된 scrubber 적용
- full·redacted·metadata와 archive 이전에서 usage 정규형을 검증하고 raw 요청·응답·공개 ID를 새 필드에 보존하지 않음

## 0.10.0 - 2026-07-31

### 저장 정책과 복구

- 보관 기간, 채팅별 개수, 전체 대략적 용량을 `기간 → 개수 → 용량` 순서로 적용하는 preview-first 정리 정책 추가
- 각 채팅의 최신 정상 스냅샷과 방금 저장한 항목을 보호하고, 정책을 만족할 수 없으면 초과 상태와 용량을 별도로 표시
- ST DevTools 데이터의 대략적 용량과 브라우저 오리진 전체의 사용량·quota를 분리해 표시
- 누락 참조·유효한 고아 레코드·잘못된 인덱스·구버전 중복/충돌 컨테이너 진단 추가
- 유효한 고아 레코드는 재인덱싱하고 검증된 구버전 중복 컨테이너만 제거하되 손상 레코드와 충돌 원본은 보존
- 손상된 인덱스와 구버전 타임라인이 함께 있는 경우 한 번의 승인으로 인덱스·요약을 완결하고, 보존한 충돌 원본의 바이트도 용량 정책에 포함
- 시작·백그라운드 정리에서는 채팅별 개수를 조용히 줄이지 않고 기간·용량만 유지하며, 개수 축소는 설정의 미리보기·확인을 거치도록 변경

### 개인정보 모드와 백업

- 새 캡처에 `전체 원문`, `원문 제거본`, `메타데이터만` 모드 추가
- 원문 제거본은 프롬프트 관련 문자열을 길이·바이트·SHA-256 placeholder로 대체하고, 메타데이터 모드는 프롬프트 구조를 제거
- 원문 제거본·메타데이터만 허용하는 공유용 내보내기와 원문 seed 재등장 시 실패하는 최종 검사 추가
- 항목별 개인정보 모드를 보존하는 스키마 v2 archive, 엄격한 크기·깊이·필드·digest 검증과 안전한 v1 이전 추가
- 기본 병합, 동일 digest 건너뛰기, ID 충돌 시 둘 다 유지/건너뛰기와 archive별 확인 문자열을 요구하는 전체 교체 추가
- 가져오기를 배타적 저장 작업으로 직렬화하고 read-back 검증 실패 시 원시 저장 상태 전체를 rollback
- full backup의 비공개 스냅샷은 원래 로컬 채팅 파티션을 무결성 검증 대상에 포함해 복원하고, 안전 공유용 archive에는 원시 파티션을 기록하지 않음
- rollback 자체가 실패하면 복원 성공으로 표시하지 않고 별도 실패 상태를 반환하며, 가져오기 뒤 보관 초과분은 자동 삭제하지 않고 정리 경고로 안내

### 성능과 진단

- 100개 이상 타임라인·소스 목록의 가상 렌더링과 키보드 위치 복원 추가
- 검색·diff·규칙 검사를 제한된 module Worker에서 실행하고 timeout·취소·stale 결과 폐기와 메모리 전용 LRU cache 적용
- 두 진단 보고서의 허용된 집계·스냅샷 메타데이터 변화 비교와 범위·버전 비호환 경고 추가

### 검증

- 보관 순서·보호 항목·오래된 preview 거부, 1,000개 이상 인덱스의 제한된 무결성 진단과 구버전 충돌 원본 보존 테스트
- 개인정보 모드의 비가역 경계, 공유용 누출 검사, 혼합 privacy archive와 변조·과대 입력 거부 테스트
- 병합·교체·동시 캡처 직렬화·read-back 실패·원시 rollback 테스트
- 실제 `SnapshotStore`를 이용한 비공개 스냅샷 full backup 왕복, 파티션 변조 거부와 rollback 실패 상태 테스트
- Worker 종료·취소·시간 초과·stale cache, 5,000개 가상 목록과 진단 비교의 비공개 ID 미노출 테스트

## 0.9.2 - 2026-07-31

### 구조 provenance 스키마 v6

- 각 소스에 구조 위치의 사용 가능 상태와 JSON pointer·메시지 번호·role·원본 값 범위·최종 프롬프트 범위를 함께 기록
- 같은 위치를 중복 제거하고 소스별 최대 50곳까지만 보존하면서 전체 위치 수와 생략 여부를 별도로 기록
- 새 캡처에서 설정 프롬프트·요청 메시지·도구·멀티모달 조각의 실제 payload 위치를 연결하고, 과거 스냅샷에는 당시 기록하지 않은 위치를 추정하지 않는 `이전 스냅샷 · 위치 기록 없음` 상태 적용
- 전송 API·프롬프트 형식·생성 유형, SillyTavern에서 선택한 생성 소스와 그 근거 위치, 확인할 수 없는 upstream provider를 `providerTrace`로 분리
- 명시적인 요청 설정 근거가 있는 assistant prefill과 마지막 assistant 메시지 위치로 추정한 후보를 `확정`·`추정`으로 구분

### 비교 정확성

- 동일한 소스의 role·depth·position·활성 상태·프롬프트 순서 변경을 내용·토큰 변화와 분리해 표시
- 실제 요청 포함 여부가 바뀐 소스는 추가·삭제로 유지하되, 내용이 같아도 배치 메타데이터만 달라진 경우를 별도 변경으로 표시
- 동일 world·UID 로어북 항목의 내용·키·위치·목록 순서 변화를 활성화·제거와 구분해 타임라인과 비교 화면에 표시

### 지연 마이그레이션과 손상 격리

- 스키마 v1~v5 레코드를 읽을 때 v6으로 변환한 결과를 개별 레코드에 한 번만 다시 저장하고 경량 인덱스·완료된 저장 요약의 바이트 수를 증분 보정
- 정상 v6 레코드는 다시 직렬화하거나 쓰지 않아 일반 타임라인 읽기 경로의 추가 비용 방지
- 변환할 수 없는 레코드는 삭제하거나 덮어쓰지 않고 같은 타임라인의 정상 스냅샷과 격리
- 손상 레코드의 원문·식별자를 UI 상태로 전달하지 않고 현재 불러오기 범위에서 읽지 못한 개수만 안내

### UI와 검증

- 탐색기 원문 안에 기본 접힘 구조 위치 상세와 근거 위치 생략 안내 추가
- 컨텍스트에 선택한 생성 소스와 upstream 확인 상태를 분리한 생성 경로 카드 추가
- 비교 화면에 배치 설정 변경 표와 동일 UID 로어북 변경 카드를 추가하고 좁은 화면에서 세로 배치
- 구조 위치 정규화·상한·레거시 정직성, provider trace, prefill 상태, 메타데이터·로어북 diff, 지연 저장·손상 원본 보존과 UI 비노출 회귀 테스트 추가

## 0.9.1 - 2026-07-31

### 비교 정책 V2

- 그룹의 동작·검사 범위와 이름 해석 규칙을 분리해 하나의 그룹 동작을 여러 규칙과 수동 지정에서 재사용
- 전체·프리셋·캐릭터·채팅 정책 프로필과 `채팅 → 캐릭터 → 프리셋 → 전체` 적용 순서, 같은 범위 우선순위와 하위 프로필 fallback 추가
- 수동 지정 우선, 이름 규칙의 첫 일치 우선순위를 유지하면서 입력 중 일치 수 미리보기와 여러 프롬프트 일괄 지정 추가
- 기존 V1 이름 규칙·수동 지정을 전역 V2 프로필로 결정적으로 이전하고, 새 캡처부터 프리셋 API와 채팅 소유자를 포함한 불투명 범위 지문값 저장
- v0.9.1 이전 스냅샷은 전역 정책과 기존 raw 범위 fallback을 유지하지만 당시 캡처되지 않은 프리셋 API·캐릭터·그룹 소유자 범위는 추정하지 않음

### 검토와 변경 미리보기

- 내용 원문을 포함하지 않는 의미 기반 검사 결과 키와 소스 순서·캡처 위치 변화에 강한 보조 소스 지문값 추가
- 검사 결과에 `유효`, `오탐`, 적용 프로필별 `항상 무시`, 세션 한정 `이번만 숨김`과 복원 목록 추가
- 좁은 적용 프로필을 항상 무시의 기본 범위로 사용하고 정확한 유효 판정은 더 넓은 무시보다 우선 적용
- 정책 적용·판정·무시 변경을 최대 200건까지 보존하는 로컬 감사 기록 추가
- 저장 전 변경 전후 검사 결과, 새로 나타남·사라짐·유지됨, 제외 비교와 소스별 정책 배정 변화 미리보기 추가

### 가져오기·내보내기와 안전성

- 규칙 설정·비교 정책과 선택적인 검토 판정을 버전 지정 JSON으로 내보내고 검증 후 하나의 초안으로 가져오는 흐름 추가
- 프롬프트 원문과 감사 기록은 내보내지 않으며 포함되는 프로필·패턴·수동 지정 식별 정보 안내 추가
- 1 MiB 입력·768 KiB 정규화 결과, 깊이·노드·프로필·그룹·규칙·수동 지정·검토 수 상한 추가
- 알 수 없는 필드·prototype 오염 키·중복 ID·끊어진 그룹 참조·위험 정규식·정규화 데이터 손실을 거부
- 가져오기 검증 실패는 기존 저장값을 변경하지 않고, 최종 적용 실패 시 네 localStorage 키의 백업 복원을 시도하며, 성공한 초안도 사용자가 `설정 적용`을 누르기 전에는 영구 저장하지 않도록 변경

### UI와 성능

- 정책 미리보기 결과를 변경 revision별로 캐시하고 변경이 없으면 추가 분석을 생략
- 미리보기 소스 카드를 100개로 제한하고 닫힌 정책 영역은 지연 렌더링
- 정책으로 제외된 비교의 정확한 전체 건수는 유지하되 상세 레코드와 DOM은 100건으로 제한하고 생략 건수를 표시
- 수동 지정 선택·결과 카드에는 프롬프트 이름만 표시하고 내부 식별자는 숨김
- 저장된 창 크기·위치를 현재 viewport 안으로 보정하고 430px 화면에서 정책 편집기와 긴 도움말의 가로 넘침 방지

### 검증

- 전체 자동 테스트 244개와 JavaScript 58개 문법 검사를 통과
- 로컬 브라우저 샌드박스에서 프로필·그룹·이름 규칙·다중 수동 지정·변경 미리보기·저장 후 재접속을 검증
- 오탐·이번만 숨김·프로필별 항상 무시의 재적용 차이, 감사 기록, 설정 재적용과 밝은/어두운 테마를 검증
- 430×900 모바일과 1280×720 데스크톱 화면에서 패널·정책 편집 컨트롤·긴 도움말의 viewport 경계와 콘솔 오류를 검토

## 0.9.0 - 2026-07-31

### Rule Inspector V3

- 명시적인 지시를 대상·행동·속성·값·긍정/금지·범위·조건·예외·우선순위 원자로 구조화
- 각 원자에 source ID·label·type·role·position·depth, 원문/최종 문자 범위, 추출 방식과 수치 신뢰도 보존
- 지시·참고·대화 산출물·tool 데이터·멀티모달 표시·최종 집계 소스의 검사 capability 분리
- 언어·형식·긍정/금지·역할 충돌을 실제 원자 쌍과 연결 클러스터로 생성
- 무조건 명시 지시는 `확정`, 조건·예외 지시는 `후보`, 역할 양립 판정은 `근거 부족`으로 구분
- 기존 대안 그룹·내부 무시·범주별 비교 정책을 원자 관계 생성 단계에 적용
- 예시·인용·fenced code block의 지시 표현을 제외하고 대화 기록·tool schema·멀티모달 표시의 오탐을 차단

### UI와 성능

- 규칙 검사에 기본 접힘 `지시 구조 분석 V3` 요약과 소스 capability·원자 상세를 추가
- 원자별 대상·처리·우선순위·범위·role·position·depth·추출 위치·방법·신뢰도와 접힌 실제 문구 근거 표시
- finding에 확정·후보·근거 부족 배지, 사람이 읽을 수 있는 충돌 묶음과 관계 근거 표시
- 원자 500개·충돌 관계 200개·우선순위 경고 100개 분석 상한과 원자 카드 100개 렌더 상한 추가

### 검증

- 조건부 언어, 양립 가능한 역할, 인용·예시, fenced code block, tool schema와 대화 기록을 포함한 익명화 한국어 golden corpus 추가
- golden corpus 정밀도·재현율 95% 이상, 모든 관계의 source·range·method·confidence와 비교 정책 연동 검증
- 300개 지시 소스 스트레스 테스트에서 분석 상한 적용과 약 30ms 내 완료 확인
- 자동 테스트 184개와 JavaScript 파일 47개의 문법 검사 통과
- 로컬 브라우저 샌드박스에서 430px·밝은/어두운 테마·접기 구조·근거 이동·콘솔 오류를 검토

## 0.8.12 - 2026-07-31

### 수정

- 설정 적용 중 전체 타임라인 새로고침까지 기다리느라 적용 버튼이 오랫동안 비활성 상태로 남고 패널이 멈춘 것처럼 보이던 문제 수정
- 테마만 변경해도 저장 데이터와 타임라인을 다시 읽던 불필요한 작업 제거
- 구버전 배열 스냅샷의 보관 축소 미리보기에서 모든 원문을 최신 스키마로 변환하던 병목 제거

### 성능과 안전성

- 설정 적용 상태를 `적용 중…`으로 명시하고 저장 작업의 성공·실패·취소 여부와 관계없이 버튼 상태를 복구
- 불러오기·보관 범위 변경에 필요한 새로고침은 모달을 닫고 버튼을 복구한 뒤 한 번만 비동기로 실행
- 구버전 배열 데이터는 삭제 수량 계산 시 ID·시간 메타데이터만 정렬하고, 승인 뒤에도 삭제될 과거 항목은 변환하지 않은 채 최신 보관분만 개별 레코드로 이전

### 검증

- 자동 테스트 176개와 JavaScript 파일 44개의 문법 검사 통과
- 20,000자 템플릿이 든 구버전 스냅샷 100개 모의 데이터에서 보관 축소 미리보기가 약 818ms에서 약 18ms로 단축됨을 확인
- 삭제될 구버전 레코드의 변환 경로가 실행되지 않는 회귀 테스트 추가
- 로컬 브라우저 샌드박스에서 테마 전용 적용의 타임라인 무재조회, 설정 모달 재진입 시 적용 버튼 활성화, 불러오기 변경 후 단일 비동기 재조회를 확인

## 0.8.11 - 2026-07-31

### 수정

- 구버전 스냅샷의 긴 매크로 템플릿을 마이그레이션할 때 브라우저 정규식 엔진의 스택 한계를 넘으면 설정 저장과 타임라인 갱신까지 실패하던 문제 수정
- 내부 정규식 오류에 프롬프트 원문이 포함되어 긴 설정 오류 알림으로 노출되지 않도록 사용자 알림을 고정된 안내 문구로 변경
- 큰 템플릿 패턴은 정규식 실행 전에 길이를 제한하고, 정규식 생성뿐 아니라 실제 매칭 시점의 지연 컴파일 오류도 안전하게 처리

### 추가

- 설정 모달에 `실리태번 테마에 맞춤`, `항상 밝게`, `항상 어둡게` 패널 테마 선택 추가
- 기존 설정에는 자동 테마를 기본 적용하고 사용자가 선택한 테마를 보관·불러오기 설정과 함께 저장

### 검증

- 자동 테스트 175개와 JavaScript 파일 44개의 문법 검사 통과
- 기존에 재현되던 약 20,000자 매크로 템플릿 마이그레이션이 예외 없이 약 3ms 안에 안전하게 미확인 처리됨을 확인
- 로컬 브라우저 샌드박스에서 자동·밝게·어둡게 전환과 재실행 후 설정 유지, 좁은 화면의 설정 모달 배치를 확인

## 0.8.10 - 2026-07-31

### 추가

- 상단 설정 모달에 `채팅별 보관 수`와 `불러올 스냅샷 수`를 별도 항목으로 제공
- 신규 프로필의 기본값을 채팅별 보관 30개·자동 불러오기 20개로 설정
- 보관 수를 낮추기 전에 영향받는 채팅 수·삭제될 스냅샷 수·대략적 용량을 표시하고 사용자가 승인한 뒤 정리

### 안전성과 성능

- v0.8.9 불러오기 설정 또는 기존 스냅샷이 있는 프로필은 보관 100개를 승계해 업데이트만으로 과거 데이터가 삭제되지 않도록 보호
- 불러올 수를 보관 수 이하로 자동 제한하고 불러오기만 바꿀 때는 저장 데이터를 건드리지 않도록 분리
- 삭제 영향 계산과 보관 정리가 스냅샷 원문 대신 채팅별 경량 인덱스를 사용하도록 구현
- 모든 채팅의 보관 정리와 동시에 들어오는 캡처를 전역 저장 잠금으로 직렬화하고 저장 요약을 증분 갱신
- 레거시 배열 정리, 동시 캡처, 설정 v1→v2 승계와 인덱스 전용 삭제 미리보기 회귀 테스트 추가

### 검증

- 자동 테스트 173개와 JavaScript 파일 44개의 문법 검사를 통과
- 8KiB 스냅샷 2,000개·채팅 20개의 메모리 모의 저장소에서 보관 30개 영향 계산 약 1.2ms, 오래된 1,400개 정리 약 2.4ms 측정
- 430px 로컬 브라우저 샌드박스에서 v0.8.9 보관 100개 승계, 보관·불러오기 자동 상한 연동, 삭제 확인 취소·승인, 정리 후 전체/로드 수와 저장 요약 갱신을 확인
- 밝은·어두운 테마 설정 모달의 가로 넘침 없음, Esc 포커스 복귀와 콘솔 경고·오류 없음 확인

## 0.8.9 - 2026-07-31

### 추가

- 상단 아이콘 영역에 톱니바퀴 설정 버튼과 키보드 포커스가 격리되는 설정 모달 추가
- 현재 채팅에서 자동으로 불러올 최신 스냅샷 수를 1~100개로 지정하고 브라우저에 저장하는 설정 추가(기본 20개)
- 전체 보관 수와 현재 불러온 수를 타임라인 안내·목록·성장 그래프에 구분해 표시
- 채팅별 전체 배열을 스냅샷별 개별 레코드와 경량 인덱스로 옮기는 중단 후 재시도 가능한 저장 구조 마이그레이션

### 성능

- 새 캡처마다 최대 100개 스냅샷 배열 전체를 읽고 다시 쓰던 경로를 대상 레코드 1개와 경량 인덱스 갱신으로 변경
- 저장 요약 재구축이 새 저장 구조의 스냅샷 원문을 읽지 않고 인덱스의 개수·대략적 크기만 합산하도록 변경
- 완성된 저장 요약을 읽을 때 전체 IndexedDB 키 열거를 생략
- 닫힌 탐색기 원문, 컨텍스트 요청 JSON, 소스별·전체 문자 diff를 펼칠 때 처음 생성하도록 지연 렌더링
- 한 스냅샷 안의 동일 문자열은 토큰 계산 결과를 재사용해 최종 프롬프트 중복 계산 제거

### 안전성과 검증

- 기존 배열 저장 데이터는 새 인덱스 쓰기가 성공한 뒤에만 제거하며, 중간 실패 시 다음 읽기에서 같은 ID로 다시 마이그레이션
- 요청한 최신 N개 레코드만 읽는 계약, 개별 저장 경로, 마이그레이션 중단 복구, 인덱스 전용 요약과 토큰 계산 재사용 회귀 테스트 추가
- 약 615KiB 스냅샷 100개의 structured-clone 모의 저장소에서 누적 저장 약 240~275ms, 최근 20개 읽기 약 4.5~5.5ms, 전체 100개 읽기 약 24~28ms 측정
- 자동 테스트 167개와 JavaScript 파일 44개의 문법 검사를 통과
- 로컬 브라우저 샌드박스에서 설정 적용·새로고침 유지, 12개 중 최근 5개 로드, 430px 모달 오버플로·Esc 포커스 복귀, 지연 렌더링과 콘솔 오류 없음 확인

## 0.8.8 - 2026-07-30

### 수정

- 패널 열기·새로고침·스냅샷 lifecycle 갱신마다 모든 채팅의 스냅샷을 읽고 전체 JSON 크기를 다시 계산하던 성능 회귀 수정
- 현재 채팅 타임라인을 먼저 렌더링하고 저장 요약은 타임라인 탭에서만 비차단으로 갱신하도록 변경
- 기존 저장 데이터의 최초 용량 보정은 약 8ms 작업 단위로 양보하며 실행하고 동시에 들어온 보정 요청은 한 작업으로 병합
- 최초 보정 이후 채팅·스냅샷 수와 대략적 용량을 추가·교체·삭제 시 증분 메타데이터로 갱신
- 스냅샷 갱신 직후 저장 요약을 기다리느라 UI 렌더링이 지연되거나 여러 전체 스캔이 겹칠 수 있던 문제 수정
- 요청 본문을 정제한 결과에서 prompt payload를 재사용하고 prompt-ready 대체 payload와 활성 로어 정제를 필요한 시점에 한 번만 수행하도록 캡처 중복 순회 감소

### 검증

- 빠른 저장 요약이 timeline 값을 읽지 않는 계약, 동시 보정 병합, 증분 추가·교체·삭제, 느린 요약과 무관한 현재 타임라인 렌더링 회귀 테스트 추가
- 229.1MB·스냅샷 2,000개 합성 데이터에서 빠른 요약 0.21ms, 백그라운드 최초 보정 중 최대 event-loop 간격 24.2ms 측정
- 20MB 요청 정제 벤치마크에서 중복 순회를 제거해 111.3ms에서 49.1ms로 단축
- 자동 테스트 157개와 JavaScript 파일 42개의 문법 검사를 통과

## 0.8.7 - 2026-07-30

### 추가

- 타임라인에 IndexedDB·브라우저 영구 저장·임시 메모리 backend, 전체 채팅·스냅샷 수와 대략적 저장 용량 표시
- 저장 인덱스에서 누락된 타임라인과 규칙·비교 정책·창 설정까지 찾아 지우는 모든 ST DevTools 로컬 데이터 삭제 기능
- 검색 입력 debounce와 정규식 256자 제한, 고위험 패턴 차단, module Worker 격리 및 800ms 시간 제한
- 토큰 형태 값, Authorization, URL query 비밀값과 PEM 개인 키 제거 및 캐릭터·설정 프롬프트·로어북·payload까지 적용되는 캡처 전체 정제
- 복사·JSON·TXT·Markdown 내보내기 전 원문 경고, 포함 필드 안내와 직렬화 후 대략적 파일 크기 확인
- 요청 본문 캡처·prompt-only 시간 초과와 생성 시작·종료·사용자 중단을 분리한 캡처 상태

### 수정

- 스냅샷 스키마를 v5로 올리고 기존 스냅샷의 요청·생성 상태를 안전한 기본값으로 마이그레이션
- 같은 채팅의 저장·삭제·전체 삭제가 교차하지 않도록 저장 변경 작업을 전역 잠금으로 직렬화
- 생성 종료 상태가 뒤늦게 같은 스냅샷에 반영돼도 사용자가 보고 있던 다른 스냅샷 선택을 바꾸지 않도록 수정
- 저장 인덱스가 손상되거나 누락돼도 용량 요약과 전체 삭제가 실제 timeline 키를 기준으로 동작하도록 수정
- 비교 정책 정규식 입력 길이를 제한하고 검사할 프롬프트 이름 길이도 상한을 적용

### 검증

- 저장 backend·용량·누락 인덱스 전체 삭제, 캡처 lifecycle, 민감 정보 정제, 내보내기 미리보기, 정규식 취소·시간 제한 회귀 테스트 추가
- 2,000개 결정론적 정규식 입력과 200개 중첩 캡처 구조를 이용한 fuzz 회귀 테스트 추가
- 자동 테스트 146개와 JavaScript 파일 42개의 문법 검사를 통과
- 로컬 브라우저 샌드박스에서 module Worker 검색, 위험 정규식 차단, 영구·임시 저장 상태, 캡처 lifecycle, 내보내기 안내, 키보드 이동·포커스 복원과 밝은·어두운 430px 레이아웃을 확인하고 콘솔 오류가 없음을 확인

## 0.8.6 - 2026-07-30

### 수정

- 도움말 아이콘을 조금 작게 조정하고, 비교 정책 상위 도움말에 섞여 있던 `대안 그룹`·`내부 무시` 설명을 각 `그룹 동작` 입력 옆 도움말로 이동
- 스냅샷 선택기와 스냅샷 목록 제목에서 불필요한 도움말 아이콘 제거
- 프롬프트 성장 그래프에서 최신 점은 경고색, 사용자가 살펴보는 점은 강조색으로 분리하고 최신 점의 접근성 이름을 명시
- 스냅샷 선택 시 전체 화면을 다시 그리지 않아 목록·키보드 포커스를 유지하고 연속 선택할 수 있도록 수정
- 지연된 새로고침 결과가 다른 채팅의 화면을 덮거나 삭제 재시도가 전환 후 채팅을 대상으로 실행될 수 있던 비동기 경쟁 조건 수정

### 추가

- 스냅샷 목록의 개별 체크박스, 모두 선택, 선택 개수와 `선택 삭제` 기능
- 여러 스냅샷을 같은 채팅 잠금 안의 단일 read-modify-write로 처리하는 저장소 연산
- 채팅을 바꾸면 이전 채팅의 다중 선택 상태를 자동 폐기하고 삭제·비우기 재시도를 원래 채팅에 고정하는 안전장치

### 검증

- 다중 삭제의 선택 ID 제거·나머지 순서 유지·빈 타임라인 인덱스 정리와 지연된 채팅 새로고침·삭제 재시도 회귀 테스트
- 430px 로컬 브라우저 샌드박스에서 가로 넘침, 도움말 배치, 목록 펼침 유지, 두 항목 삭제, 선택 초기화, 최신·검토 점 색상과 최신 그래프 점 갱신을 확인하고 콘솔 오류가 없음을 확인

## 0.8.5 - 2026-07-30

### 수정

- 도움말과 화면 설명에서 여러 문장을 하나의 문단으로 이어 표시하던 문제를 수정하고, 마침표로 끝나는 각 문장을 독립 문단으로 분리
- 각 설명 문장과 `예:`·`예시:`·`ex.`·`e.g.` 예시 문단 사이에 한 줄 간격 적용
- 버전 번호·소수점·URL 내부의 마침표와 `ex.`·`e.g.` 약어는 문장 끝으로 잘못 분리하지 않도록 보호

### 검증

- 한국어 연속 문장, 예시 접두어, 버전·소수·URL을 포함한 순수 문단 분리 회귀 테스트 추가

## 0.8.4 - 2026-07-30

### 수정

- SillyTavern의 Chat Completion 분류값 `openai`를 실제 제공자로 표시하던 문제를 수정하고, 캡처된 `chat_completion_source`를 우선 표시
- Text Completion의 `textgenerationwebui` 전송 계열도 실제 text generation 유형을 구분해 표시
- 요청 설정 이벤트가 없는 대체 캡처에서도 현재 Chat Completion·Text Completion 제공자를 새 스냅샷에 보존
- 스냅샷 TXT·Markdown과 개인정보 제외 진단 JSON·Markdown에도 API 경로와 생성 제공자를 분리해 기록
- 긴 도움말이 패널 오른쪽·아래 경계를 벗어나지 않도록 열릴 때마다 패널 내부 좌표와 최대 크기를 계산
- 컨텍스트의 `구조화 입력 상세 통계` 제목이 다른 접기 제목과 달리 오른쪽에 붙던 정렬 문제 수정

### 개선

- 탐색기 안내, 스냅샷 선택, 타임라인 목록, 비교 기준, 로어북 변화, 컨텍스트 상세, 규칙 근거와 검색 옵션까지 도움말 범위 확대
- 타임라인 성장 그래프는 전체 기록 중 최근 10개만 표시하고, 각 점의 hover·focus·click에서 시각과 사용 토큰을 확인하도록 개선
- 모바일 그래프 점에 넓은 투명 터치 영역을 추가하고 키보드 Tab 진입점은 한 개로 제한
- 그래프에서 선택한 점의 상세 정보를 고정하고 `이 스냅샷 열기`로 기존 탐색 동작을 유지
- 일반 UI 텍스트는 공백과 문장 경계를 우선하고 한글 단어를 임의로 쪼개지 않도록 공통 줄바꿈 규칙 적용
- 도움말의 일반 설명과 `예:` 문장을 독립 문단으로 나누고 문단 사이 한 줄 간격 적용
- 12개 스냅샷과 여러 Chat Completion 제공자를 포함하는 반응형 UI 샌드박스 fixture 확장

## 0.8.3 - 2026-07-29

### 수정

- 탐색기·타임라인·비교·컨텍스트·규칙 검사 제목의 상시 노출 설명을 도움말 아이콘으로 옮기고, PC hover·키보드 focus·모바일 click으로 확인하도록 변경
- 타임라인 토큰 변화 그래프는 기본 펼침으로, 스냅샷 목록은 개수만 보이는 기본 접힘으로 재배치
- 소스별 프롬프트 비교가 설정상 비활성 또는 실제 요청 미포함 소스의 내용 변화를 추가·삭제·수정으로 표시하지 않도록 수정
- 활성 프롬프트가 다음 스냅샷의 실제 요청에서 빠진 경우는 실제 변화이므로 삭제 상태를 유지
- 비교 정책의 이름 규칙·수동 지정·현재 스냅샷 미리보기를 각각 독립 접기 영역으로 변경
- 수동 지정 프롬프트 선택 목록에는 사용자에게 필요한 이름만 표시하고 내부 identifier는 값으로만 유지
- 접힌 비교 정책 본문이 내부 레이아웃 규칙 때문에 화면에 남을 수 있던 CSS 접기 우선순위 문제 수정

## 0.8.2 - 2026-07-29

### 수정

- SillyTavern의 프롬프트 정의 배열 대신 실제 `prompt_order`를 우선 사용해 Prompt Manager 배치 순서와 활성 상태를 캡처
- Prompt Manager에 배치되지 않은 드롭다운 전용 프롬프트가 탐색기·통계·규칙 안내에 소스로 섞이지 않도록 수정
- 탐색기를 프리셋 프롬프트·그 외 요청 소스·최종 프롬프트 그룹으로 나누고 프리셋 순번과 요청 포함 상태를 표시
- 연결 상태 용어와 설명을 사용자 관점으로 바꾸고 카드 색상이 정확도나 우선순위를 뜻하지 않음을 안내
- 의미가 불분명한 화살표를 펼친 카드의 `최종 위치 보기` 버튼으로 교체하고 접힌 최종 프롬프트까지 확실히 열어 이동하도록 수정
- 타임라인 진단·가져오기·삭제 기능, 토큰 그래프, 컨텍스트 상세 통계·내보내기·원문을 기본 접힘 영역으로 정리
- 비교 탭에 기준→비교 대상의 추가·삭제·수정 의미를 설명하고 소스 원문과 전체 문자 diff를 기본 접힘으로 변경
- 컨텍스트의 가장 큰 소스 계산에서 설정 비활성 및 실제 요청 미포함 프롬프트를 제외
- 규칙 검사의 미연결 소스 안내가 비교 그룹 결과가 아니라 활성 소스 중 연결 위치를 못 찾은 항목만 뜻함을 명시

## 0.8.1 - 2026-07-29

### 수정

- 선택 탭과 비선택 탭의 hover·focus 시각 상태를 분리하고 호스트 테마의 버튼 테두리가 탭 위에 겹치지 않도록 수정
- 좁은 패널에서 타임라인·컨텍스트·규칙 검사 버튼 글자가 세로로 쪼개지지 않고 버튼 단위로 다음 행에 배치되도록 수정
- 밝은 테마의 프롬프트 연결 상태, 소스 추가·수정, 이전 턴 토큰 변화 및 규칙 심각도 색상을 고대비 팔레트로 변경
- 타임라인 변화 배지에 중첩 적용되던 투명도를 제거
- 검색의 정규식·대소문자 구분 옵션을 하나의 가로 그룹으로 배치
- 밝은 테마와 415px 패널을 재현하는 UI 샌드박스 fixture 및 상태색 대비 회귀 검사 추가

## 0.8.0 - 2026-07-29

### 추가

- 설정 프롬프트의 설정상 활성 상태와 실제 요청 포함 상태를 분리한 소스 메타데이터
- 프롬프트 이름을 `{group}`·`{option}`으로 해석하는 사용자 정의 템플릿 규칙
- 임의 구분자와 named capture를 지원하는 정규식 이름 규칙
- 프롬프트 식별자 우선 수동 그룹 지정과 이름 규칙보다 높은 우선순위
- `대안 그룹`·`내부 무시`·`일반 그룹` 동작 및 검사 범주별 적용
- 이름 규칙 순서 변경, 규칙 활성화·삭제, 현재 스냅샷 정책 미리보기
- 제외된 소스·비교 쌍·인식된 그룹·다중 활성 그룹 경고의 투명한 표시
- 대안 그룹 내부 비교는 제외하되 그룹 밖 프롬프트와는 계속 비교하는 소스 쌍 정책
- 같은 대안 그룹에서 여러 옵션이 실제 요청에 포함될 때 쌍별 충돌 대신 단일 그룹 경고
- 설정 프롬프트와 연결되지 않은 실제 system/developer 요청 조각의 별도 소스 보존
- 템플릿·정규식·수동 우선순위·범주·활성 상태·외부 비교·잘못된 설정 회귀 테스트
- 세 언어 옵션과 비교 정책을 직접 검증할 수 있는 v0.8 UI 샌드박스 fixture

### 수정

- 규칙 검사가 평탄화된 전체 프롬프트를 한꺼번에 훑지 않고 실제 검사 대상 소스별로 분석하도록 변경
- 비활성 설정 프롬프트, 실제 요청에 미포함된 프롬프트, 대화 기록과 최종 합성 결과를 지시 충돌 비교에서 제외
- 설정 프롬프트 문자열 연결을 역할별 payload 메시지 범위로 제한해 다른 역할의 우연한 동일 문자열 귀속을 방지
- 비활성 프롬프트가 활성 payload의 동일 문자열을 자신의 근거로 가져가지 못하도록 수정
- 정책으로 제외된 비교에도 검사 범주·그룹·제외 이유를 표시
- 근거 범위가 없는 결과에서는 사용할 수 없는 최종 프롬프트 이동 버튼을 숨김
- 모든 비교 대상 규칙이 최종 합성 결과나 대화 기록을 옵션으로 분류하지 않도록 수정

## 0.7.0 - 2026-07-29

### 추가

- 같은 채팅과 채팅 인덱스의 저장 작업을 직렬화하는 비동기 잠금
- 캡처·읽기·삭제·비우기·전체 진단 읽기 실패의 패널 알림과 재시도
- 실패한 캡처를 동일 스냅샷 ID로 다시 저장하는 중복 방지 재시도
- 저장된 모든 채팅을 `chat-N`으로 익명화한 메타데이터 진단 JSON·Markdown
- 진단 JSON의 크기·스키마·개수·개인정보 플래그·금지 필드 검증과 검토 전용 가져오기
- OpenAI 지원 모델의 32px 패치·512px 타일 이미지 토큰 추정
- Anthropic 28px 이미지 패치와 표준·고해상도 상한 추정
- Google 768px 이미지 타일, 오디오·비디오 초당 토큰 추정
- 멀티모달 추정의 산정 방식, 하한·상한·산정 불가 및 커버리지 표시
- `{{...}}`·`${...}`·`<%...%>`·`<<...>>` 치환 템플릿 provenance 후보와 신뢰도
- 스키마 v4 및 이전 스냅샷의 provenance·멀티모달 통계 자동 마이그레이션
- 동시 저장·인덱스 정합성·실패 재시도·안전한 진단 가져오기·공식 추정 공식 회귀 테스트

### 수정

- 최종 스냅샷 삭제 또는 타임라인 비우기 뒤 채팅 인덱스에 빈 항목이 남지 않도록 정리
- 최종 삭제와 새 저장이 동시에 실행돼도 살아 있는 채팅 인덱스가 사라지지 않도록 잠금 범위를 확장
- 멀티모달 placeholder의 일반 텍스트 토큰 수를 실제 provider 토큰처럼 보이지 않도록 제거
- 템플릿 후보가 매크로 앞뒤의 불필요한 최대 500자까지 포함하지 않도록 고정 문자열 범위를 제한
- 가져온 진단 보고서를 타임라인에 복원하는 기능으로 오해하지 않도록 검토 전용 상태를 명시

## 0.6.0 - 2026-07-27

### 추가

- 공개 `request_id`·`generation_id`·`completion_id`·`response_id`가 있을 때 prompt/settings 이벤트를 ID로 연결
- ID 일치·FIFO·prompt-ready 단독 캡처를 스냅샷과 UI에 명시
- tool/function schema·호출·결과 전용 프롬프트 소스와 구조화 입력 통계
- 이미지·오디오·비디오·파일 입력의 전용 소스와 안정적인 placeholder
- 대용량·민감 미디어 data URL의 저장 전 생략과 생략 경로 기록
- NFKC·대소문자·공백·zero-width 차이를 허용하는 보수적 정규화 provenance
- 프롬프트·요청 본문·요청 ID 값을 포함하지 않는 현재 채팅 전체 타임라인 진단 JSON·Markdown
- 모달 포커스 트랩, 닫은 뒤 포커스 복원, 탭 좌우/Home/End 이동
- 성장 그래프 포인트의 Tab·방향키·Enter·Space 탐색
- 스키마 v3과 이전 스냅샷 자동 마이그레이션
- 요청 연결·멀티모달 생략·구조화 소스·진단 개인정보·접근성 회귀 검증

### 수정

- Chat Completion 최종 표시에서 tool/function call payload가 누락되지 않도록 구조화 필드를 포함
- tool schema·tool choice·legacy function 필드를 sampler 설정 목록에서 분리
- 공식 이벤트에 요청 ID가 없는 현재 SillyTavern에서는 기존 FIFO를 유지하되 연결 방식을 숨기지 않도록 변경
- 탭을 바꿀 때 이전 화면의 스크롤 위치 때문에 새 화면 상단이 잘리지 않도록 수정

## 0.5.0 - 2026-07-24

### 추가

- 규칙 검사 결과에서 관련 소스 카드로 이동하고 모든 관련 소스를 함께 강조
- 최종 프롬프트의 정확한 정규식 근거 범위를 강조하는 이동 동작
- 규칙별 활성화와 컨텍스트·대형 소스·중복 문장 임계값 설정
- 손상된 설정 정규화와 기본값 복원
- 긍정/금지 지시 충돌과 앞선 지시 무시 문구 검사
- API·이벤트 경계별 데이터 기반 캡처 호환성 fixture
- 100개 스냅샷 보관·분석 및 다중 범위 성능 회귀 테스트
- 구현 현황 문서와 v0.5 실사용 체크리스트

### 수정

- 동시에 도착한 여러 settings-ready 이벤트가 서로 다른 pending 캡처를 예약하도록 수정
- 같은 Text Completion 요청 객체가 settings-ready와 generation-data 이벤트에 연속 전달되어도 다음 pending 캡처를 소모하지 않도록 수정
- `max_completion_tokens`를 출력 예약량 계산에 반영
- 고정 20ms 대기에 의존하던 캡처 테스트를 조건 기반 대기로 변경
- 타임라인 화면에서 사용하지 않는 소스 diff를 지연하고 범위 분할을 sweep 방식으로 개선
- 모든 규칙이 꺼진 상태를 검사 결과 없음과 구분
- 비어 있거나 `null`인 임계값 설정을 최소값이 아닌 안전한 기본값으로 복원
- 응답 언어 규칙의 근거 범위가 다음 줄의 지시까지 확장되지 않도록 문장 경계를 제한
- 띄어 쓴 한국어 금지 표현을 긍정·금지 충돌로 이중 판정하지 않도록 수정
- 반복된 역할 선언 뒤에 나타난 다른 역할도 충돌로 검출하도록 수정
- 규칙의 관련 소스에서 실제 최종 근거 범위와 연결되지 않은 소스를 제외
- sweep 범위 분할에서도 겹친 소스의 원본 순서를 유지
- 한국어 임계값 설정 라벨이 좁게 줄바꿈되지 않도록 반응형 그리드를 조정

## 0.4.0 - 2026-07-24

### 추가

- 원본 소스와 최종 프롬프트 문자 범위의 상호 강조
- 소스 카드에서 대응하는 최종 프롬프트 위치로 이동하고 반대 방향으로 탐색
- 타임라인의 턴별 토큰 증감과 프롬프트 성장 그래프
- 이전 턴 대비 활성화·제거 로어북 항목 표시
- 안정적인 소스 식별자를 사용한 소스별 추가·삭제·수정 비교
- 임의의 두 스냅샷 간 로어북 변화 비교
- 타임라인 스냅샷 개별 삭제
- 타임라인·로어북·소스 비교·겹치는 문자 범위·삭제 동작 단위 테스트

### 수정

- 과거 스냅샷에 로어북 배열이 없어도 타임라인을 안전하게 표시
- 전체 프롬프트 비교에서 DiffMatchPatch를 사용할 수 없는 환경의 대체 표시 유지

## 0.3.0 - 2026-07-24

### 추가

- Chat Completion 및 Text Completion 요청 설정 이벤트 우선 캡처
- 캡처 이벤트·경계·대체 캡처 여부를 기록하는 스키마 v2
- 캡처된 요청 본문과 프롬프트를 제외한 생성 설정 표시
- API 키·토큰·비밀번호·쿠키 등 민감 정보 필드 저장 전 제거
- 캐릭터 시스템 프롬프트·후처리 지시·예시 대화·첫 메시지·깊이 프롬프트 소스
- 소스별 최종 프롬프트 문자 범위
- v0.1/v0.2 스냅샷 자동 마이그레이션
- 요청 캡처·민감 정보 제거·마이그레이션·문자 범위 테스트

### 수정

- 요청 설정에서 확인한 모델과 출력 토큰 값을 우선 사용
- 같은 요청 설정 이벤트의 동기적 후속 변경을 반영하도록 리스너 반환 직후 객체를 복제
- Context Inspector에서 프런트엔드→백엔드와 provider 서버 후처리의 경계를 명시

## 0.2.0 - 2026-07-24

### 추가

- 한국어 중심 문자열 카탈로그와 전체 사용자 UI 번역
- 읽기 전용 상태 배지
- 중복·반복 문장 검사
- 언어·출력 형식·역할 충돌 검사
- 높은 컨텍스트 사용률 및 과도한 단일 소스 검사
- 소스 연결 미확인 항목 진단
- 치명적·경고·정보 심각도별 규칙 검사 화면
- 규칙 검사 및 기존 한국어 호환성 테스트

### 수정

- 투명도가 포함된 SillyTavern 테마 변수에 의존하지 않도록 변경
- 현재 글자 명도를 기준으로 밝은/어두운 불투명 패널 팔레트를 적용
- 스냅샷 TXT 및 Markdown 내보내기의 사용자 문구를 한국어로 변경
- v0.1.0 스냅샷의 영문 기본 소스 이름을 한국어로 표시

## 0.1.0 - 2026-07-24

### 추가

- SillyTavern 확장 manifest와 Magic Wand 실행 메뉴
- 읽기 전용 Chat Completion 및 Text Completion 캡처
- 활성화된 로어북 항목 캡처
- 채팅별 로컬 스냅샷 타임라인
- 토큰 수와 보수적인 소스 연결 상태를 표시하는 프롬프트 탐색기
- SillyTavern 내장 DiffMatchPatch 기반 임의 스냅샷 비교
- 사용량 통계·복사·내보내기를 포함한 컨텍스트 검사
- 일반 검색·대소문자 검색·정규식 검색
- 드래그·크기 조절·상태 기억이 가능한 테마 대응 창
- 의존성 없는 구문 검사와 단위 테스트
- GitHub Actions 검사 워크플로
